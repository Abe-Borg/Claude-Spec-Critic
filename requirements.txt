# Runtime lock for the app (what the Windows build freezes). Test-only packages
# live in requirements-dev.txt, which starts with `-r requirements.txt`.
annotated-types==0.7.0
anthropic==1.4.0
anyio==4.14.2
certifi==2026.4.22
charset-normalizer==3.4.7
customtkinter==5.2.2
darkdetect==0.8.0
distro==1.9.0
docstring_parser==0.18.0
h11==0.16.0
httpcore2==2.12.0
httpx2==2.12.0
idna==3.19
jiter==0.14.0
# keyring: OS credential store for the API key (optional at runtime — the app
# falls back to its key file — but declared in pyproject and bundled by the
# Windows build). Its pure-Python dependency chain is pinned below; the
# OS-specific backends (pywin32-ctypes on Windows, SecretStorage/jeepney on
# Linux) resolve through keyring's own environment markers.
keyring==25.7.0
jaraco.classes==3.4.0
jaraco.context==6.1.2
jaraco.functools==4.6.0
more-itertools==11.1.0
# lxml: imported directly by src/output/report_exporter.py (and by python-docx).
lxml==6.1.0
packaging==26.2
platformdirs==4.9.6
pydantic==2.13.3
pydantic_core==2.46.3
pypdf==6.10.2
python-docx==1.2.0
regex==2026.4.4
# requests (+ certifi / charset-normalizer / idna / urllib3 above): transitive
# dependency of tiktoken, which downloads its rank file through it.
requests==2.33.1
sniffio==1.3.1
tiktoken==0.12.0
tkinterdnd2==0.4.3
# truststore: transitive dependency of httpx2/httpcore2 (the SDK's HTTP stack
# builds its TLS context from the OS trust store through it), and the frozen
# app calls truststore.inject_into_ssl() at startup (packaging/windows/
# app_entry.py) so the stdlib urllib paths — the self-updater — also trust a
# corporate TLS-intercepting proxy's certificate installed in the OS store.
truststore==0.10.4
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.6.3
