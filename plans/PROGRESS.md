# Spec Critic plan — progress tracker

This file records what's done and what's next. The instructions live in
[`spec-critic-implementation-plan.md`](spec-critic-implementation-plan.md).

**Coding agents:** update this file in the same pull request as your work (plan, Part 1). Master's
copy of this file is the truth: a chunk counts as done only once the PR that marks it DONE is merged.

---

## ▶ Right now

| | |
|---|---|
| **Next chunk** | **S12 — Keep every edit location, part 2: sidecar writer and reports** (WP-06B) |
| **Last finished** | S11 — Keep every edit location, part 1: occurrence model and applier reader (WP-06B) |
| **Last merged PR** | [#385](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/385) (S11) |
| **Overall** | 11 of 25 chunks done |

**Prompt for the next session** (paste it into a new Claude Code session on this repository):

```text
Continue the Spec Critic implementation plan.

Next chunk: S12 — Keep every edit location, part 2: sidecar writer and reports (WP-06B).

Start from the latest master. Read CLAUDE.md, then plans/PROGRESS.md, then Part 1
and chunk S12 in plans/spec-critic-implementation-plan.md, and its packages in Part 4.
Do only this chunk. If PROGRESS.md names a different next chunk, follow PROGRESS.md.
Open one PR. When I tell you it's merged, give me the prompt for the next session.
```

---

## Status of every chunk

Status words: **TODO** · **IN PROGRESS** · **PARTLY DONE** (the next session continues it) ·
**DONE** · **DONE (not evaluated)**, for experiments run without a live evaluation.

| Chunk | What it does | Packages | Status | PR |
|---|---|---|---|---|
| S01 | Record the starting point; build shared test fixtures | WP-01 | DONE | [#375](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/375) |
| S02 | Stop merging different findings; applier refuses ambiguous files | WP-06A, WP-07 | DONE | [#376](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/376) |
| S03 | Fix the false structure alerts and the text checks | WP-04 | DONE | [#377](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/377) |
| S04 | Make the report chat recover from errors | WP-12 | DONE | [#378](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/378) |
| S05 | Stop caching "couldn't verify" as an answer | WP-10 | DONE | [#379](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/379) |
| S06 | Size big requests for the model that runs them | WP-08 | DONE | [#380](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/380) |
| S07 | Show when compliance coverage is incomplete | WP-09 | DONE | [#381](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/381) |
| S08 | Keep paid repair batches recoverable | WP-14 | DONE | [#382](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/382) |
| S09 | Count every paid attempt exactly once | WP-15 | DONE | [#383](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/383) |
| S10 | Read Word content controls, fields, and smart tags | WP-02 | DONE | [#384](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/384) |
| S11 | Keep every edit location, part 1: occurrence model and applier reader | WP-06B | DONE | [#385](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/385) |
| S12 | Keep every edit location, part 2: sidecar writer and reports | WP-06B | TODO | |
| S13 | Route specs by their own SECTION heading | WP-05 | TODO | |
| S14 | Read Word automatic numbering | WP-03 | TODO | |
| S15 | Respect rate-limit timing | WP-11 | TODO | |
| S16 | Make tracing optional; keep keys out of the environment | WP-13 | TODO | |
| S17 | Keep the verifier's citations; fix the fetch instructions | WP-16 | TODO | |
| S18 | Make prompts, reports, and docs match the code | WP-17 | TODO | |
| S19 | Correctness release | release | TODO | |
| S20 | Experiment: shared project-context caching | EX-01 | TODO | |
| S21 | Experiment: schema-constrained outputs | EX-02 | TODO | |
| S22 | Experiment: model, effort, and confidence | EX-03 | TODO | |
| S23 | Experiment: evidence validation and source reuse | EX-04 | TODO | |
| S24 | Experiment: research reuse | EX-05 | TODO | |
| S25 | Experiment: cross-chunk and cross-module coordination | EX-06 | TODO | |

---

## Chunk checklists

A chunk is DONE when every box below it is ticked and its packages' acceptance criteria in Part 4 of
the plan pass. If a box turns out to be wrong, don't tick it silently: write what you did instead
under "Decisions and deviations".

From S01 on, each chunk's known defects are strict xfails in `tests/test_plan_open_defects.py`;
search it for `fixed by S0N` to find yours. A fix shows up as an `[XPASS(strict)]` failure naming the
chunk: remove that marker in the same pull request, and keep the control test next to it passing.

### S01 — Record the starting point; build shared test fixtures (WP-01)
- [x] Starting commit and offline test result recorded under "Starting point" below. Re-measure; the September 23 numbers are only a reference.
- [x] Shared DOCX fixture builders in `tests/fixtures/`. They cover the clean 3-PART spec (plan, Appendix A) and its four variants: table-only article body, automatic numbering, a truly empty article, and a truly duplicated heading. They also cover content controls (block, inline, dropdown), simple fields (a stored REF result), smart tags, hyperlinks, tracked insertions and deletions, merged and nested tables, and representative compact filenames. → `tests/fixtures/spec_docx.py`, pinned by `tests/test_spec_docx_fixtures.py`.
- [x] Clean fixtures and single-defect mutations are separate builders.
- [x] Contract pins for behavior that is already right: extraction reconstruction, unique element IDs, the meaning of legacy `pN` / `tN` IDs, and group-vs-occurrence identity as it stands today. → `tests/test_contract_pins.py`.
- [x] Every check in `plans/check_plan_status.py` becomes a test marked `xfail(strict=True, reason="open: fixed by S0N (WP-xx)")`. The script is then deleted, and this file names the new test module. → `tests/test_plan_open_defects.py` (markers also carry `raises=AssertionError`; see "Decisions and deviations").
- [x] Each converted check keeps its control case, so a detector that goes silent or a cache that stops caching can't pass as a fix. The "source hint" checks become behavioral tests, not string searches. (The Haiku cache-minimum check stays a documentation check; see "Decisions and deviations".)
- [x] The full offline suite passes, with the new tests reported as xfailed.

### S02 — Stop merging different findings; applier refuses ambiguous files (WP-06A, WP-07)
- [x] The generic "CSI number … .docx" stripping is gone. Only exact known corpus filenames are normalized, with literal escaping and clear boundaries. With no corpus context, the text is kept as is. → `FindingIdentityContext` in `src/orchestration/pipeline.py`.
- [x] One normalization context is used for review, cross-check, compliance, and finding IDs, with no global mutable state. The `rf-` / `cf-` / `lc-` prefixes are kept. → `finding_identity_context_for_submission`; an AST tripwire in `tests/test_finding_identity_normalization.py` requires every production call to pass it.
- [x] Copper and PVC stay distinct, and the same issue naming different known files still groups. Overlapping names, spaces, punctuation, uppercase extensions, unknown names, and reordered input don't corrupt identity.
- [x] Applier: a filename maps to every distinct resolved path. Two different same-named inputs are ambiguous in either order; repeating one path is not. → `applier/run.py::_index_specs`.
- [x] All file bindings and destinations are resolved before any write. Output collisions, and destinations that would overwrite any supplied source, are refused. Every held instruction appears in the receipt with a specific reason, and the exit status is non-success when anything is held. → `_plan_files`; new outcomes `FILE_AMBIGUOUS` / `DESTINATION_CONFLICT`; exit `3` (see "Decisions and deviations").
- [x] `--assist` never picks among ambiguous files. A dry run and a real run make the same decisions.
- [x] Release-note line added: some finding IDs change because the old key was wrong; existing reports are untouched.

### S03 — Fix the false structure alerts and the text checks (WP-04)
- [x] Heading candidates carry number, title, level, and position. Integer-led prose and quantities ("2 coats…", "12 inches…", "1.5 inches…") are not headings. → `HeadingCandidate` / `heading_candidates` in `src/input/preprocessor.py` (also `run_in` and a `provenance`, `"typed"` until S14).
- [x] A heading's content runs through its whole subtree, so a PART with articles is not empty. The clean 3-PART fixture and its table-only variant produce no empty or duplicate alerts; a truly empty article and a truly duplicated heading still alert. An empty PART is one alert, not one per empty article (new `empty_part` fixture mutation). → `tests/test_heading_structure.py`.
- [x] Stale-citation suppression uses only citation-related historical or rejection phrases. The three WP-04B examples and the "shall not deviate" / "cannot depart" forms are flagged. Genuine "previous edition" / "superseded" contexts stay suppressed, and sentences with several citations are tested. → three cue tables plus neighbor-citation bounds; `TestCitationRelatedSuppression` in `tests/test_preprocessor_policy.py`.
- [x] ASCE/SEI, the optional word "Standard", Unicode dashes, and 2- and 4-digit edition years are recognized. The California long-form references go into the California module's vocabulary only. → `_ASCE7_PATTERN` / `_asce7_edition_key`; `src/modules/california_k12_mep.py`.
- [x] Bare TBD is detected once, with no double count alongside `[TBD]`. Keyword boundaries hold (EDITION is not EDIT, SELECTED is not SELECT), `TBDF-200` stays clean, and the `TBD-200` policy is written down. → `PLACEHOLDER_PATTERNS` comment and CLAUDE.md §5: `TBD-200` is an identifier.
- [x] File naming: separated, compact, and SECTION-prefixed names are recognized. Unknown names don't hide a mixture, and when no style dominates a neutral mixture notice is issued. → `detect_inconsistent_file_naming`; "dominant" means used by more than half of the CSI-named files.
- [x] Rule IDs, alert order, and alert limits are unchanged, and so are the location-aware modules' policies. Changed goldens are reviewed one by one. → `TestAlertContractUnchanged`; two golden lines changed, both the naming alert's `context` (see "Decisions and deviations").

### S04 — Make the report chat recover from errors (WP-12)
- [x] A Node test harness runs the exact script the exporter ships (extracted the same way the CSP test extracts it) against scripted event streams. → `tests/fixtures/chat_harness.js` + `chat_harness.py`; `tests/test_html_chat_behavior.py` (92 tests). The harness checks the extracted bytes against the report's CSP hash.
- [x] API error events, transport and reader failures, malformed required data, and premature EOF all reach the chat's state. A response without a valid terminal event is not treated as complete. → a response counts only at `message_stop` with a stop reason and every block closed; the `try` guards only `JSON.parse`.
- [x] Split UTF-8 characters, arbitrary chunk boundaries, LF and CRLF separators, and multi-line data frames reconstruct correctly. → also lone CR endings, a CR at a chunk end, a fatal decoder flushed at the end, and an unterminated final event (never dispatched).
- [x] Invalid or incomplete tool arguments never become `{}`. Stop reasons, the continuation limit, and the tool-round limit are handled visibly. → every stop reason, including `model_context_window_exceeded` and unknown ones (see "Decisions and deviations").
- [x] Committed history never holds a `tool_use` without its `tool_result`, under one documented transaction model. Partial text is shown as interrupted and never replayed as a complete answer. → rollback: a turn commits only on `end_turn` / stop sequence (module docstring, CLAUDE.md "HTML report + Ask AI", handbook ch. 22).
- [x] Stop, New Chat, and a model change can't let an old response write into a newer conversation, and the controls are always restored. → also Forget key and leaving the page; tested with streams that ignore the abort.
- [x] Citation deltas stay with their text block and survive the next request.
- [x] The API key lives in page memory only. The legacy `sc_api_key` storage entry is removed and never re-imported, Forget Key clears memory, and a storage failure doesn't break chat.
- [x] Escaping and CSP-hash tests still pass, and the key policy in CLAUDE.md's "HTML report + Ask AI" section is updated.

### S05 — Stop caching "couldn't verify" as an answer (WP-10)
- [x] Real time and batch share one verdict-classification contract. A malformed or missing verdict after an ordinary end turn is an operational failure that keeps its known usage, not an UNVERIFIED. Refusal, max tokens, malformed tool input, and unexpected stops are each handled explicitly. → `verifier.classify_verification_turn` + `_stamp_verdict_result` / `_failure_result`, called by both transports; `VerificationResult.outcome`; `tests/test_verdict_classification_contract.py` (every turn outcome through both transports, compared field by field).
- [x] One cache-eligibility predicate is used at write, read, and disk load. Only grounded conclusive verdicts that meet the source and quote rules are reused. UNVERIFIED, failed, budget-exhausted, and local results never are. Invalid timestamps and non-finite numbers are rejected record by record. → `verification_cache.cache_ineligibility_reason`; `_record_timestamp` / `_persisted_payload_problem`; `tests/test_verification_cache_eligibility.py`.
- [x] Legacy UNVERIFIED rows are ignored individually and valid conclusive rows still load. There is no blanket flush. → counted in `stats()["rejected_on_load"]` and the run's load log line; no schema bump (still v4).
- [x] Within a run, a well-formed UNVERIFIED is shared once among equivalent in-flight findings; parse failures, local results, and budget failures are not shared. Followers settle correctly when the leader succeeds, is uncertain, fails, or is cancelled, and they add no billed usage. → `pipeline._shareable_verdict` requires `outcome == "verdict"`; WP-10 section of `tests/test_verification_singleflight.py`.
- [x] A later run retries an earlier UNVERIFIED under the normal escalation policy. → `TestALaterRunRetries` (real `verify_finding`, real disk round trip, HIGH finding escalates again).
- [x] Empty or whitespace-only sources are rejected on both transports and in `classify_status`. A DISPUTED backed only by `""` is not DISPUTED. → `source_grounding.is_substantive_source`, shared by the grounding invariant, the cache predicate, and `classify_status`.
- [x] The CLAUDE.md "Budget-exhaustion sentinel" sentence that says the grounded guard drops every UNVERIFIED is corrected, along with any related cache text. → also the two CLAUDE.md claims that the cache refused a quote-less DISPUTED, and handbook ch. 10 (see "Decisions and deviations").

### S06 — Size big requests for the model that runs them (WP-08)
- [x] A small request-budget result records the count, count source, model, input ceiling, output reserve, fit decision, and any unavailability reason. It is built from the same inputs as the real request. → `RequestBudget` in the new `src/core/request_budget.py`; the counting form comes from the request's own params (`count_request_from_params`), and each pass builds its request once (`build_cross_check_request`, `build_compliance_request`, `build_review_request`).
- [x] Count-API results are called estimates, not "exact". Malformed or missing counts never become a trusted zero. → `tokenizer.validated_input_tokens` accepts only a positive `int`; logs, errors, the GUI gauge, the digest dialog, and the docs say "API estimate".
- [x] When the count API is off or unavailable, model-specific padding applies to every locally counted part of the request, tool overhead included. → `local_request_tokens` counts system, messages, each tool's JSON, `tool_choice`, and a 600-token tool-use allowance; Opus 5 / Opus 4.8 now pad by 1.45× (see "Decisions and deviations").
- [x] The single-call vs chunked decision comes from that result, and every final chunk is checked too. Counts are cached only per model and per exact request shape. → `request_budget_for` in both passes, a recheck inside `run_cross_check` / `run_compliance_check`, and a process-wide cache keyed by the SHA-256 of the complete counting form, model included.
- [x] An oversized CSI group is subdivided deterministically. An item that can't be split is reported as unanalyzed and never truncated. → `chunked_pass.plan_chunks`: contiguous parts found by bisection, ids `<group>:<n>`; a spec that cannot fit is a not-analyzed entry that never reaches the runner.
- [x] Completed chunks are kept when another chunk fails. Reduced cross-chunk coordination and failed or skipped coverage are surfaced. → a scope note heads the combined summary, every failed or skipped chunk names its files ("Not analyzed: …"), not-analyzed parts count in `chunk_skips` (the banner's red flag), and the log names them.
- [x] Preflight respects the per-call concurrency gate without holding it across a whole multi-chunk pass. → each count call takes the pass's `call_gate` for that call only, after the client and method are resolved; `tests/test_collection_call_gate.py` counts count and stream acquisitions.
- [x] The batch extended-output threshold uses the same count source, and real time stays on its non-extended path. → the builder reads the preflight's cached estimate for the identical shape (else the padded count) before choosing the cap and rechecks the fit; the real-time gate reads the same count, and real-time requests pin `force_allow_extended_output=False`.
- [x] All nine WP-08 acceptance cases pass with stubbed counts. → `TestAcceptance1` … `TestAcceptance9` in `tests/test_request_budget.py`, driven through the real passes with scripted count answers.

### S07 — Show when compliance coverage is incomplete (WP-09)
- [x] The expected coverage set is the grounded, controlling, non-process requirements; UNVERIFIED research stays advisory. The prompt and examples agree with that set. → `compliance_checker.expected_coverage_ids`; rows for any other id are ignored and counted. `<coverage_rules>`, `<final_task>`, the schema's `coverage` description, the tool description, and the examples all say "one entry per controlling requirement id, none left out; [UNVERIFIED] and [PROCESS] items never get entries" (two DC compliance goldens moved, reviewed line by line).
- [x] Returned rows are normalized against known IDs and omitted IDs are computed. An omitted row becomes a synthetic "unclear / not assessed" row with a reason and an origin marker, never "represented" or "missing". → `completeness.reconcile`: every row carries `origin` (`model` / `synthetic`), `assessment` (`full` / `partial` / `none`), `reason`, and `also_reported`; rows come out in expected order.
- [x] The result carries the expected count, the returned count, the omitted IDs and their count, and a completeness flag. Behavior with zero expected items is documented. → `CoverageCompleteness` on `ReviewResult.coverage_completeness` (`expected_count`, `returned_count`, `omitted_ids` / `omitted_count`, `complete`, `state`); zero expected is a completed, complete `no_applicable_items` result (CLAUDE.md "Compliance coverage completeness").
- [x] Execution status stays separate from completeness; no new chunk status is added that would drop findings. Completed findings survive failed or skipped chunks. → `cross_check_status` keeps its closed set; the engine's two compliance hooks became one `finalize` hook that sees every planned chunk, after the engine sets the status and error.
- [x] An ADD that depends on the requirement being absent everywhere is held as report-only, with the reason, when any relevant chunk wasn't assessed. → `_settle_additions`: held through `_demote_to_report_only` with a reason starting "Absence not established" that quotes the proposed text; never dropped (see "Decisions and deviations" for the cases beyond the plan's).
- [x] A prominent partial-analysis notice appears in the DOCX and HTML reports, diagnostics, JSON/profile output, and program reports. → shared helpers in `report_exporter` (banner row, red notice in the banner and the Requirements Coverage table, NOT ASSESSED cells, subtitle), sidecar / `.profile.json` / HTML payload keys, `diagnostics.compliance_pass_extra` and a run-log warning, and a program roll-up (`completeness.combine`).

### S08 — Keep paid repair batches recoverable (WP-14)
- [x] A structured collection outcome separates "there are reportable primary findings" from "the remote job and its repairs are finished". It distinguishes no repair needed, pending, temporarily unreachable, consumed, and conclusively unusable. → `orchestration/collection_outcome.py` (stdlib-only): `CollectionOutcome.reportable` vs `remote_settled` / `provisional`; `RepairOutcome.state` is `not_needed` / `pending` / `unreachable` / `consumed` / `unusable` / `not_submitted`, and only `pending` and `unreachable` are outstanding. It rides `CollectedBatchState` → `PipelineResult.collection_outcome` (a program exposes `collection_outcomes`).
- [x] The repair batch ID and request map are persisted, and legacy saved state is read conservatively. → Stamping (already on master) became one locked load → stamp → save, `batch_resume.record_repair_batch`, because concurrent program children lost each other's stamps. Re-attaching needs only the saved maps and the primary request map's file names. A record with no repair fields is a legacy record and clears only when it names this run; an unreadable or newer-schema record, or one naming a repair this run never settled, is never deleted; a legacy id-only repair record rebuilds its map from the request map, and a mismatched id maps nothing.
- [x] One cleanup decision is used by the GUI, the CLI, single-module runs, and program runs. State is kept while a repair is pending or unreachable, an all-failed run stays recoverable, and a valid zero-findings run may clear. Run and batch identity are checked before clearing. → `decide_saved_state_cleanup`, applied through `batch_resume.apply_saved_state_cleanup` by the GUI's single-module and program branches and by `scripts/recover_batch.py`; `clear_saved_state_for` deletes only a record that names this run's batch and repair ids. `PARITY_SCENARIOS` (`tests/fixtures/batch_service.py`) is checked through the CLI, the headless driver, and the GUI.
- [x] A reportable primary result with a pending repair is shown as provisional. Dependent paid stages (verification, cross-check, compliance, drawing impact) wait for the repair, and the report says which stages are waiting. → `defer_collection_stages` / `provisional_batch_result` in both drivers; a routed program collects in two phases and holds every module's stages and its drawing-impact pass while any module's repair is outstanding. A red banner row and notice (both exporters), the sidecar's `provisional` / `collection`, the diagnostics event, the trace, the GUI's amber finish, and the review errors say so.
- [x] Resume collects the same repair ID with no duplicate submission, and tests count actual paid calls. Program children keep their unresolved state independently. → `tests/test_repair_recovery.py` and `tests/test_repair_recovery_gui.py`, through a fake batch service that counts repair submissions and every dependent pass apart from free polls and reads: a pending repair survives a restart and is collected once, with each stage run once, on the CLI and in the GUI; each child's repair is stamped on its own partition and re-attached on its own. A bare-id recovery whose repair is outstanding saves a record when the slot is free, a kept record gets back any repair id whose first save failed (`restamp_repair_batches`, found in review), and **Recover batch…** resumes a saved id from its record.

### S09 — Count every paid attempt exactly once (WP-15)
- [x] Each attempt gets a usage record: operation, model, transport, token and cache categories, search usage, and a stable identity (batch ID + custom ID + role). → New `src/core/attempt_usage.py`: `AttemptUsage` (operation, role — primary / repair / retry / escalation / fallback — transport, model, known or unknown usage, the six cache keys, searches and fetches, identity, scope, outcome). A batch item is `batch:<batch id>:<custom id>:<role>`, a synchronous response `message:<message id>`; a request that raised has none and is never deduplicated.
- [x] Primary and repair attempts are both kept even when the repair's findings replace the primary's. Failed, truncated, and malformed attempts with usage stay billable, and unknown usage stays unknown and labeled. → `collect_review_batch_results` takes the primary records from the results as retrieved, before the merge, and the repair pass returns its records on `RepairOutcome.attempts` at every exit; the combined `ReviewResult.call_usage` holds all of them. An errored / canceled / expired batch item is a known zero (not billed); a pending or unreachable repair, a request that raised, and any result never read are unknown — counted and named, never priced. The same model covers verification retries, fallbacks, detaches, and escalations, cross-check's parse retry, real-time review calls that raised, and Haiku triage (see "Decisions and deviations").
- [x] Each operation has one billing input (attempts or an aggregate, never both). Real-time totals are unchanged for an equivalent scenario, and shared followers add nothing. → `diagnostics._billing_record`: an event is priced from its attempt records when present (its flat fields are display only), else from its flat totals as one aggregate. The real-time runner's rows carry the same usage as before, one per call; cache replays and shared followers carry no records and bill nothing.
- [x] The batch discount, cache TTL categories, cache reads, and search fees are each applied once, and repeated collection doesn't double count. → Each attempt is priced once on its own model and transport (a real-time fallback in a batch run pays standard rates); an attempt id seen twice counts once, and a read copy wins over an unread one. The billing input is taken before the event caps and never evicted, so a long run no longer loses its earliest spend.
- [x] Recovery reports separate earlier batch spend from spend caused by the recovery itself, and estimates are labeled as estimates. → `scope` (`run` / `earlier`: a resumed submission's primary batch and a re-attached repair) feeds `cost_summary.by_scope`; `cost_summary_lines` — the one wording, shared by the text export, the GUI Diagnostics window, and `scripts/recover_batch.py` — reads "Estimated cost (USD): … an estimate from the recorded usage at list prices, not an invoice", with "Earlier batch spend" and "This collection's own spend" lines, spend by operation, and what it could not price.

### S10 — Read Word content controls, fields, and smart tags (WP-02)
- [x] `w:sdt` / `w:sdtContent`, `w:smartTag`, and `w:fldSimple` are traversed structurally. Only stored field results are read; field instructions are never read as prose or executed. → One walk, `_paragraph_segments`, reads every paragraph on every surface (body, table cells, headers and footers, text boxes, notes). Inline controls, smart tags, custom XML, `w:dir` / `w:bdo`, hyperlinks, and simple fields are descended by name, never by a descendant-text search, and their property children (`w:sdtPr` and the like) are never read. `_FieldState` follows complex-field nesting within a paragraph, so an instruction is never read, and neither is a nested field's result that is only an argument inside an outer instruction. Nothing is executed or updated. Block controls and custom XML blocks are read where they sit.
- [x] Accept-All revision rules apply at every depth, including inside controls and hyperlinks. Nothing is emitted twice and source order is preserved. → Insertions and move destinations are kept and deletions and move sources dropped at every depth: a control inside a deletion disappears, and an insertion inside a hyperlink is kept. Two duplicates found on the way were removed: each text box Word saves twice (a DrawingML shape and its VML copy) is now read once, and a Word table of contents, a generated copy of the headings, is skipped (see "Decisions and deviations").
- [x] Legacy `pN` / `tN` IDs keep their meaning: a wrapped table doesn't renumber ordinary tables. Newly readable wrapped content gets its own ID namespace. → `pN` is still physical body child N, `tN` the N-th direct body table, and `r<n>` a table's n-th own row. Content read from inside a block control gets ids with a `cc` step: `cc<N>p<i>` / `cc<N>t<i>r<r>` in the body, `<table>cc<k>r<i>` for rows a control wraps, and `s<n>hcc<k>p<i>`, `tb<b>cc<k>p<i>`, `fn<id>cc<k>p<i>` in the other stories, all with `container_type="content_control"`. `tests/test_contract_pins.py` checks every `cc` id against an independent reading of the XML.
- [x] Applier: each new ID either resolves deterministically or is explicitly unsupported. An edit touching an unsupported wrapper is refused, even when matching text exists elsewhere. → Every `cc` id classifies as the new `ElementKind.CONTENT_CONTROL`, which is reported and never written (`WRITABLE_KINDS`). The writer matches against the extractor's own segments and refuses any match reached through a control, field result, hyperlink, smart tag, custom XML, direction wrapper, or another author's revision, each with its own reason, and any span that would move a field character, a control, a tracked deletion, or an anchored object. With no confirming element id, a copy of the target text in a control, text box, or note makes the match ambiguous: the editable copy no longer wins by default. The finding's section may single out one copy, and if that copy is not editable the edit is refused. The assist tier can't choose a copy the applier doesn't write.
- [x] Text-bearing structures that are still skipped produce a warning, without invented completeness numbers. → Equations (Office Math), embedded documents (`altChunk`), legacy drop-down form fields (Word records the choice as a position in the field's list, not as text in the paragraph), and tables inside a control within a table row or cell each add one extraction warning that names the kind and counts the elements, for example "Spec contains 2 equations (Office Math) whose text was not extracted for review. Verify visually." It never estimates how much text was lost. The gaps that predate S10 are listed in CLAUDE.md but don't warn (see "Decisions and deviations").
- [x] The supported and unsupported surfaces are listed in CLAUDE.md "DOCX supplemental content extraction", and the handbook row in "Known-wrong statements" is fixed. → A table there lists each structure, whether it is read, and its ids; the applier's side is under "The applier is a separate program". `handbook/04_input.md` has a new currency note with the corrected "still not extracted" list; automatic numbering stays on it for S14.

### S11 — Keep every edit location, part 1: occurrence model and applier reader (WP-06B)
- [x] Display groups are separated from executable occurrences. There is one occurrence per validated target (element identity + instruction identity), genuine duplicate emissions collapse, and indistinguishable anchors stay one uncertain occurrence. → `group_findings(findings, *, module_id, element_index)` in `src/orchestration/pipeline.py`: one `FindingOccurrence` per file and target (the named element — `validated` against the reviewed text via `element_index_from_specs`, else `claimed` — plus the instruction, an ADD's side included); members at one target collapse into `members`; every member of a file naming no usable element is one `unresolved` occurrence, never absorbed into a located one. `edit_occurrences` is a run's executable view, one per occurrence id (content twins collapse, the others kept in `also_reported_by`).
- [x] Occurrence IDs are stable and don't depend on input order or presentation counters. Program entries include module identity, and a missing original stays explicitly missing. → `compute_occurrence_id` = `oc-` + 12 hex of `(module_id, finding_id, file, target)`; `group_id` is the finding id (the `grp-0000::000::file` counters are gone). A file with no recorded original is `missing_original`, `original_finding=None`, and `executable_proposal()` gives it no element and no anchor.
- [x] Every consumer of `finding_id` and `EditEntry.key` is audited: receipts, filters, chat links, and conflict detection. → Table under "Decisions and deviations". Receipts carry `occurrence_id` / `location_basis` / `related_entries`; `--only` also takes occurrence ids; conflict detection keys on resolved places, never on ids (an AST test pins that no applier decision reads `EditEntry.key`). Found for S12: the HTML report's finding anchors repeat across modules and across content twins, so the chat's `navigate_to_section` reaches only the first — two new strict xfails.
- [x] The applier reads sidecar schemas 4, 5, 6, and 7, and `is_program` recognizes both 5 and 7. Legacy 4 and 5 behave exactly as before, and unknown versions are still refused. → `applier/sidecar.py` defines the 6/7 contract (module docstring): a required `occurrence_id` and `location_basis`, the basis/element rules, `module_id` required in 7, the unique key `(module_id, occurrence_id)` with every copy of a repeat refused. `LEGACY_SCHEMA_VERSIONS`, `OCCURRENCE_SCHEMA_VERSIONS`, `PROGRAM_SCHEMA_VERSIONS`; `is_program` / `is_occurrence_aware`. Legacy entries read field for field as before (pinned).
- [x] The applier detects incompatible edits to one region and holds them visibly. Every target is still resolved before any mutation. → `DocumentEditor.plan` decides every edit's span or insertion gap, and raises every writer refusal, against the unmutated document; `applier/conflicts.settle` holds different instructions for one place as `EDIT_CONFLICT` (each naming the others) and writes identical ones once (`DUPLICATE`); `apply_planned` writes from the planned positions. Every permutation of a mixed sidecar gives one document and one outcome per entry.
- [x] The sidecar writer still emits 4 and 5; the new writer lands in S12. → Byte-identical to master: `edit_sidecar._legacy_file_occurrences` keeps the pre-S11 expansion, pinned by the new golden `tests/goldens/legacy_sidecar_payloads.json` (12 scenarios captured on master); S12's strict xfail for p4/p8 still xfails.

### S12 — Keep every edit location, part 2: sidecar writer and reports (WP-06B)
- [ ] The sidecar writes schema 6 (single module) and 7 (program). Each entry has an `occurrence_id` and a documented unique-entry key that includes module provenance.
- [ ] The same issue at p4 and p8 gives two entries and two correct tracked changes; a duplicate emission at p4 gives one; two files with two locations each give four. Cross-module entries don't collide, and conflicting edits are held.
- [ ] Both exporters show occurrence locations, and receipts account for every occurrence.
- [ ] An end-to-end test runs report → sidecar → applier → receipt and keeps every executable occurrence.
- [ ] The CLAUDE.md sidecar section and the handbook row in "Known-wrong statements" are updated, and a release-note line is added.
- [ ] Found by S11's audit: finding anchors in the HTML report are unique across the modules of a program and across content twins (the two `TestFindingAnchorsAreUnique` strict xfails), so the chat's `navigate_to_section` can reach every finding.

### S13 — Route specs by their own SECTION heading (WP-05)
- [ ] The SECTION heading is extracted from a bounded opening region, and only heading-shaped text counts. Section number and title are carried with provenance on the extracted spec, and assignment and routing share one extraction rule.
- [ ] A compact leading filename number is accepted only when the body heading corroborates it. The guards against dates, project numbers, NFPA references, and embedded numbers are kept.
- [ ] Contradictory strong filename and body evidence gives an explicit ambiguous result.
- [ ] Unsupported Division 27/28 scopes and legacy fire-alarm corroboration are unchanged.
- [ ] Distinct inputs with colliding basenames are rejected before submission at headless boundaries too, and routing provenance survives saved state and resume.
- [ ] `210500.docx` + SECTION 21 05 00 routes to fire suppression, `211313.docx` + its wet-pipe heading is supported, and related-section references can't override the real heading.

### S14 — Read Word automatic numbering (WP-03)
- [ ] Numbering is resolved from `numPr`, `numId`, `abstractNum`, level text, starts, overrides, restarts, and style-inherited numbering. Counters are kept per document and per list instance.
- [ ] Displayed labels reach review, section attribution, and structural detection (revisit the S03 heading candidates). Context-DOCX extraction behavior is defined.
- [ ] Literal source text and synthetic label spans are kept separately, and the reconstruction contract is updated deliberately. The structural locator ignores display labels.
- [ ] Typed numbering isn't duplicated. Unsupported or ambiguous numbering produces a warning instead of a guess.
- [ ] The applier refuses edits that touch a synthetic label, while body-text edits after a label still locate. Offsets are translated only when the mapping proves them.
- [ ] A boundary test runs extraction → prompt → finding → applier, and the handbook's extraction list mentions numbering.

### S15 — Respect rate-limit timing (WP-11)
- [ ] Retry-After (in seconds or as an HTTP date) and supported millisecond headers are parsed and validated; bad values fall back to the local policy.
- [ ] Backoff is bounded and exponential with injected jitter, and never retries before a valid server floor. Both the attempt count and the elapsed time are bounded, and retry-count settings keep their meaning, zero included.
- [ ] Authentication and invalid-request errors are not retried as transient.
- [ ] Concurrency permits are taken per outbound call, continuations and escalations included, and released before sleeping. The same gate is never acquired twice in a nested way.
- [ ] Each outbound path has a documented retry owner, including batch-result retrieval and token counting, and app-owned loops run with SDK retries off.
- [ ] Tests use an injected clock, sleep, and random source; one transient failure produces the expected number of calls.

### S16 — Make tracing optional; keep keys out of the environment (WP-13)
- [ ] Trace startup and reattachment run inside the worker's lifecycle protection on fresh and resumed runs. A trace failure logs one warning and the review continues without tracing.
- [ ] A partially started recorder is disposed, and only the run that owns the global recorder can clear it. Teardown errors never hide the real error, and widgets are restored on every exit.
- [ ] With deep trace on, core requests ask for the summarized thinking display (subject to the model's capabilities). Normal-mode requests stay byte-identical, and missing thinking is never logged as returned.
- [ ] Keys entered in the GUI are never written to `os.environ`. A per-run credential or client provider is passed through orchestration and captured when the run starts. Environment keys on the command line still work, and nothing sets and restores a global variable.
- [ ] Fake keys are absent from the process environment, saved state, report payloads, and trace metadata.
- [ ] GUI tests: install `python3-tk` if possible so the skipped GUI suites run; otherwise say so in the PR.

### S17 — Keep the verifier's citations; fix the fetch instructions (WP-16)
- [ ] Native citation data is captured where the response carries it: source URL and title, tool identity, cited text, locators, the attempt and model, and whether the result was fresh, cached, or shared.
- [ ] Document-index citations resolve only through their own response documents. Unknown citation shapes are observable without dropping an otherwise valid result.
- [ ] The optional fields round-trip through the cache, legacy entries still load and are labeled honestly, and no whole documents are persisted.
- [ ] Retrieval, native attribution, and semantic support appear as separate concepts in the trace and the report.
- [ ] The fetch instructions allow a URL supplied by the user or already present in the conversation, within the tool's constraints, and still require checks of support, edition, authority, and applicability. Capability gates are unchanged, and verifier goldens are reviewed.
- [ ] No lexical-overlap threshold changes acceptance.

### S18 — Make prompts, reports, and docs match the code (WP-17)
- [ ] The Haiku cache minimum is corrected to 4,096 in `src/core/api_config.py` and CLAUDE.md, after rechecking the provider's table.
- [ ] Count wording distinguishes provider estimates, local estimates, fallback padding, context capacity, and output capacity.
- [ ] Continuation caching is documented as existing behavior.
- [ ] No-op demotion is explainable: the reason is recorded at a stable normalization step, not inside a read-only report helper, and the banner count, severity counts, and sidecar exclusion agree.
- [ ] The cross-check scope is documented: it works within a chunk and a module, and a small program is not automatically checked across disciplines.
- [ ] Banners and summaries distinguish "analysis incomplete", "verification inconclusive", "operational failure", and "no issue found".
- [ ] Price commentary is corrected: Sonnet 5 is 40% of Opus 5 and Sonnet 4.6 is 60% of Opus 4.6. Recheck the prices first.
- [ ] Thinking display is described as visibility control, not a cost reduction.
- [ ] Every row still open in "Known-wrong statements" is fixed. CLAUDE.md, the README, and the handbook match shipped behavior, and no experiment is described as enabled.
- [ ] Changed goldens are reviewed one by one.

### S19 — Correctness release
- [ ] The release-note lines collected below move into README.md "Changelog (recent)" under the new version. They are specific and make no unmeasured quality or cost claims.
- [ ] The version is bumped in every literal the release check reads (see CLAUDE.md "Windows desktop build + self-update"), and `tests/test_release_metadata.py` passes.
- [ ] The full offline suite, `python -m pip check`, and the JavaScript check (`SPEC_CRITIC_REQUIRE_HTML_TEST_TOOLS=1`) pass.
- [ ] The PR body has a short Windows smoke-test checklist for the owner to run before merging: start a review, recover a batch, use the HTML chat, and apply a sidecar to a copy.
- [ ] No tag is pushed by the agent; the PR states the exact tag command to run after merging.

### S20–S25 — Experiments (EX-01 … EX-06)
The same four boxes apply to each experiment, plus its own line.
- At session start, ask the owner whether a live evaluation is authorized. That needs a spending cap and an API key in the session's environment. Record the answer.
- Do the offline part: the investigation, any code behind a default-off switch, and offline tests using fake responses.
- Write a decision record at `plans/experiments/EX-0N-<name>.md` with dataset and configuration hashes, arm settings, measurements, spend, and the decision: enable, defer, reject, or not evaluated.
- Change no default unless the experiment's promotion criteria in Part 4 pass.

| Chunk | Common boxes done | Experiment-specific box | Decision |
|---|---|---|---|
| S20 (EX-01) | [ ] | [ ] Exact request layout captured (tools, system blocks, project context, per-file content, breakpoints, TTLs, model), and the breakpoint budget counted, resume caching included | |
| S21 (EX-02) | [ ] | [ ] First consumer chosen from measured parse failures. Strict tool arguments, forced tool use, and constrained final output kept distinct, and older saved batches still parse | |
| S22 (EX-03) | [ ] | [ ] Adjudicated dataset with held-out cases, caches isolated per arm (memory and disk), and one change tested at a time | |
| S23 (EX-04) | [ ] | [ ] Validation runs in observation mode only, source-reuse keys include the claim context, and validation and reuse are decided separately | |
| S24 (EX-05) | [ ] | [ ] Cache key built from every materially relevant input, failed or partial research never reused, a refresh path offered, and the profile's age shown | |
| S25 (EX-06) | [ ] | [ ] Observation mode behind a switch, starting within one module and then a small program. Every conflict cites both sides, and false joins and missed conflicts are measured | |

**Skipping the experiments:** if the owner starts a session with "Skip the Spec Critic experiments",
mark S20–S25 **DONE (not evaluated)** with the reason "skipped by owner", open one PR, and after it
merges print the final banner (plan, Part 1).

---

## Known-wrong statements

These say something the code doesn't do. Don't rely on them. The listed chunk fixes each one and
ticks it here.

| Fixed | Where | What it says | What's true | Fixed in |
|---|---|---|---|---|
| [x] | `CLAUDE.md`, "Budget-exhaustion sentinel" (~line 441) | "The `grounded` guard already drops every UNVERIFIED" | A grounded UNVERIFIED is cached and replayed | S05 |
| [x] | `src/core/tokenizer.py` (~lines 105, 169–170, 433), `src/orchestration/pipeline.py` (~lines 671, 686) | `count_tokens` results are "exact" | They are provider estimates | S06 |
| [x] | `handbook/04_input.md` (~line 20) | "Still not extracted" list | Content controls, field results, and smart tags are also not extracted; automatic numbering is lost too | S10 (numbering part in S14) |
| [ ] | `handbook/11_trust_model_and_output.md` (~line 28) | "The sidecar no longer under-emits" | True across files only; repeated locations in one file still collapse to one entry | S12 |
| [ ] | `CLAUDE.md`, "Prompt Caching" table (~line 633); `src/core/api_config.py` (~lines 961, 999) | Haiku's cache minimum is 2048 tokens | 4,096 for Haiku 4.5 | S18 |
| [ ] | `handbook/12_configuration_and_models.md` (~line 399), found by S01 | Haiku's cache minimum is 2,048 tokens | 4,096 for Haiku 4.5 | S18 |
| [ ] | `handbook/15_quality_engineering.md` (~line 162 and its `[^count]` footnote), found by S04 | "The suite is 49 test files holding roughly 645 test functions", offered as the order of magnitude | 150 test files and about 3,100 `def test_` functions (about 4,650 collected tests) at S04 | S18 |
| [ ] | `src/output/report_exporter.py` and `src/output/html_report_exporter.py`, the Run Diagnostics row "REPORT_ONLY demotions at parse time" and the per-finding note "demoted to REPORT_ONLY at parse time", found by S07 | Every demotion happens at parse time | The row counts every finding with a `demotion_reason`, including anchor-validation demotions made after parsing and (from S07) compliance additions held after the chunk merge; the per-finding note is still printed for anchor-validation demotions. S07 gave held additions their own note | S18 |
| [ ] | `CLAUDE.md`, "Source layout", found by S07 | The tree lists every `src/` package | `src/research/` (the requirements-research fan-out) is missing; S07 added the `compliance/` entry | S18 |
| [ ] | `handbook/02_architecture.md` (~line 108), found by S06 | "`core` imports from no other package at module-load time", with `tokenizer.py`'s lazy client import as the one exception | `core/chunked_pass.py` imports `review.reviewer` (`Finding`, `ReviewResult`) at module load, and has since the chunked-pass engine was extracted | S18 |
| [ ] | `handbook/21_realtime_transport.md`, §9 (~line 221), found by S08 | The routed-program concurrency model (concurrent preparation, the shared research permits, concurrent collection, the call semaphore) "is described in Ch 18" | Ch 18 does not describe it; only `CLAUDE.md` "Routed-program concurrency" does | S18 |

---

## Decisions and deviations

Record here, with the date and chunk, whenever a session departs from the plan, finds a package
already done, or makes a judgment call the plan left open.

- **2026-09-23, plan revision:** The original WP-17 item 8 named the wrong models. It is corrected in the plan to: Sonnet 5 is 40% of Opus 5, and Sonnet 4.6 is 60% of Opus 4.6.
- **2026-09-23, plan revision:** Sessions run one at a time in the order above, so the original plan's guidance on parallel agents, worktrees, and a separate integrator (its §4) no longer applies.
- **2026-09-24, S01 — markers carry `raises=AssertionError`.** A bare `xfail(strict=True)` treats *any* exception as the expected failure, so a check broken by a renamed function would quietly read as "still open". The deleted script reported that case as ERROR. With `raises=AssertionError`, only a failed assertion counts as the defect; probe preconditions ("the flow really reached the worker hand-off") fail through `pytest.fail`, so a probe that stops reaching its target fails loudly. The reason text is unchanged, so a search for `fixed by S0N` still finds every marker.
- **2026-09-24, S01 — controls are separate tests.** The script checked a control inside the same check. In pytest a control that failed inside a strict xfail would read as "still open", so each control is an ordinary test beside its xfail and must always pass.
- **2026-09-24, S01 — WP-05 enters at the assignment seam.** The two routing checks run a real extracted DOCX through `assignments_for_specs`, not `route_spec`. S13 carries the SECTION heading on the extracted spec, so a check that called `route_spec` directly could stay red after a correct fix.
- **2026-09-24, S01 — source hints.** Three became behavioral tests. WP-11 drives the batch-results retry loop with a real `RateLimitError` carrying `retry-after`. WP-12 runs the exporter's exact script under Node (`tests/fixtures/chat_key_probe.js`). WP-13 runs the three real GUI flows against fake apps; they skip without tkinter, and here they ran and xfailed under Python 3.12 with Tk. The fourth, WP-17's Haiku cache minimum, has no behavior to exercise: the number appears only in comments and docs, and the triage no-cache decision is right either way. It stays a documentation check, made precise. It parses each "Haiku cache minimum" claim, requires the claims to exist (so deleting them can't pass as a fix), and requires 4,096. Its control pins the behavior that matters: triage is not cached.
- **2026-09-24, S01 — more reproductions than the script had.** These were added as strict xfails too. WP-02 (S10): the block control's order, inline and drop-down control text, an unresolved drop-down reaching the placeholder detector, smart-tag text, an insertion inside a hyperlink, and an edit aimed at a control landing on a plain copy elsewhere. WP-03 (S14): numbering labels in the review prompt and section attribution. WP-04 (S03): each mutation producing only its own alert, the other WP-04A quantity phrases, the "shall not deviate" / "cannot depart" forms, and ASCE with an em dash or the word "Standard". WP-07 (S02): end to end through `apply_sidecar`, in both input orders. WP-10 (S05): a whitespace-only source. The WP-06B check belongs to S12, where the sidecar writer changes.
- **2026-09-24, S01 — two WP-01 acceptance lines wait for S03.** "Clean documents produce zero alerts" and "each mutation produces no unrelated alerts" can't pass until S03, because every PART heading is falsely flagged today. S01 changes no behavior, so both are strict xfails owned by S03. The halves that already hold are ordinary tests: each mutation raises its own alert, and the clean fixture is clean for every non-structural detector.
- **2026-09-24, S01 — no per-package suites yet.** The suggested suites (`test_extraction_content_controls.py`, `test_extraction_numbering.py`, `test_heading_structure.py`) were not created. The reproductions stay in one module so a single search finds every open defect; the fixing sessions add focused suites.
- **2026-09-24, S01 — found, for S10.** An edit aimed at hyperlink text is refused with the reason "the target text sits inside an existing tracked revision by another author". `DocumentEditor._target_paragraph` looks at every nested run, so hyperlink runs trigger the revision message. The refusal is safe, but the reason is wrong.
- **2026-09-24, S01 — fixtures checked in a real word processor.** Every ready-made fixture was opened once in LibreOffice Writer 24.2 and exported to text. The auto-numbered fixture displays "PART 1 GENERAL", "1.01 SUMMARY", "A. Provide…", and every wrapped sentinel is visible text. CI has no word processor, so the XML is pinned instead.
- **2026-09-24, S02 — known names are removed, not replaced by a placeholder.** The plan allows either ("may remove the exact known filename"). Removal keeps an id unchanged wherever the old rule was already right — a finding that names only its own file — so ids move only where the old key was wrong. `TestIdStability` pins both halves against the old rule.
- **2026-09-24, S02 — the context is derived, not stored.** Each of the three stages calls `finding_identity_context_for_submission(submission)` on the same submission (files reviewed ∪ review request map ∪ re-extracted specs) instead of carrying a context field on `CollectedBatchState`. It is a pure function of one object, so the stages cannot disagree, and no saved state changes. An AST tripwire fails if stage code passes any other context.
- **2026-09-24, S02 — a space is a token boundary.** Prose separates a file name from the words before it with a space, so a known name at the end of an unrecognized multi-word name ("Old Work Results.docx" with only "Results.docx" known) is still removed from it. Documented in the class docstring and CLAUDE.md, not pinned as desired behavior.
- **2026-09-24, S02 — new outcomes and exit 3.** "Non-success when anything is held" is read as held by the new binding and destination checks. They get their own outcomes, `FILE_AMBIGUOUS` and `DESTINATION_CONFLICT`, and a new exit status `3` that doesn't need `--strict`: the inputs are wrong, not the edits. Policy holds keep their meaning (`--strict` still ignores them), and `FILE_MISSING` still needs `--strict` as before, since supplying a subset of the specs is legitimate.
- **2026-09-24, S02 — two binding rules the plan implies but doesn't list.** A sidecar that spells one name two ways (`Spec.docx` / `spec.docx`) is `FILE_AMBIGUOUS`: names match case-insensitively, so the two can't be told apart, and before this both groups bound one file and wrote one destination twice. A sidecar `fileName` containing `/` or `\` never binds (`FILE_MISSING`, "names a path"), per WP-07 item 6.
- **2026-09-24, S02 — destination equals source now refused while planning.** It used to apply the edits in memory and then demote them to `FAILED`, and a dry run skipped the check and reported `WOULD_APPLY`, which was a dry-run/real-run mismatch. It is now `DESTINATION_CONFLICT` in both, and `test_the_source_is_never_the_destination` was updated to say so. `_apply_to_file` keeps a last check right before the save, and a test proves it on its own.
- **2026-09-24, S02 — re-running over a folder with last run's output is refused.** `--specs <dir>` sweeps in the previous `*.applied.docx`, which is then a supplied specification that the new copy would overwrite. Before, it was overwritten silently, along with any review work saved in it. The message says to move it or change `--output-dir` / `--output-suffix`. This is user-visible, so it has a release-note line.
- **2026-09-24, S02 — case policy.** Two paths are one input when `Path.resolve()` + `os.path.normcase` agree (on Windows this also absorbs case). On a case-insensitive volume that `normcase` doesn't know (macOS), the same path up to case also counts when the filesystem reports one non-zero inode. A zero inode never counts, because some filesystems report 0 for every file. Destination checks compare case-folded paths everywhere, which is the refusing direction. Found in review (Codex, P2): an existing destination can also be a supplied spec under another name, as a hard link. Saving rewrites the file in place, so that spec would change. Existing destinations are now also compared by `(st_dev, st_ino)`, against every supplied spec and against each other; a zero inode proves nothing. An existing copy that is nobody else's file is still replaced, as before.
- **2026-09-24, S02 — found: CLAUDE.md's applier test count was stale.** It said 213, but master had 221. It now says 261 and lists `test_applier_bindings.py`.
- **2026-09-24, S03 — "provenance" is the number's source, and there is only one today.** The preprocessor receives text, not the paragraph map, so no style names are available to it. `HeadingCandidate.provenance` is `"typed"` for every heading now. S14 adds automatic numbering as a second provenance, and should revisit the candidates there (as its checklist says).
- **2026-09-24, S03 — a bare integer is never a heading number.** The plan asks to exclude integer-led prose. The rule excludes every integer-led line, including a heading such as "1 GENERAL" written without the word PART. SectionFormat PART headings carry the word, so this only loses detections in formats this app doesn't target, and a missed heading can't cause a false alert. A dotted number also needs a title-shaped title: a capital first letter, no `shall` / `must`, no mixed-case sentence ending in a period, not a table row, and at most 120 characters.
- **2026-09-24, S03 — ancestor/leaf policy: report the highest empty heading.** An empty heading is reported only when its parent is not empty. Every empty heading is then covered by exactly one alert, and "report an empty PART when it has no substantive descendant content" holds even when the PART has articles. The fixtures got an `empty_part` mutation (PART 2's only article loses its only body; expected alert: `PART 2 PRODUCTS`), and the fixture oracle in `tests/test_spec_docx_fixtures.py` states the same policy from declared roles.
- **2026-09-24, S03 — two additions to "a heading's content".** Run-in text after a colon (`1.03 REFERENCES: ASTM A53`) is the heading's own content. The structure also stops at an `END OF SECTION` line and at the extractor's footnote, endnote, and header/footer blocks, so a last article with nothing before `END OF SECTION` is now flagged. Before, that line counted as its body. The text-box block does not stop the structure: a text box is anchored in the body and can hold a heading's only content. Duplicate headings are still compared across the whole file, as before.
- **2026-09-24, S03 — suppression cues are bound to the citation.** The plan asks for "citation-related historical/rejection phrases." Each cue must touch the citation (only an article and punctuation before it; only copulas, relative pronouns, and punctuation after it), except `previously` / `formerly` / `no longer` / an old-edition phrase anywhere earlier in the clause. Those lose their force when `shall` / `must` / `will` / `should` stands between them and the citation. The window is also cut at neighboring citations. That is what makes sentences with several citations come out right. Two effects go beyond the plan's list. "Previously approved submittals shall comply with 2022 CBC" now flags (it was suppressed). A few clear rejections now suppress where the old code flagged them: `instead of` / `rather than` / `in lieu of`, `supersedes` / `replaces`, `, not [per] <citation>`, and `not the <citation>`. Bare `not` is still not a cue.
- **2026-09-24, S03 — found in review (Codex, P2): a cue shared by a list.** Cutting each window at the neighboring citation hid a shared cue from all but one member of a coordinated list: "Previously, the 2019 CBC and 2019 CMC applied" still flagged the CMC. Citations joined only by a comma, `and`, `or`, `and/or`, or `&` (optionally followed by `the`) are now judged as one citation, so the cue before the list or after it covers every member; any other words between two citations still keep them apart. Fixing it exposed an older bug in both year/code detectors: in "2019 CBC, 2019 CMC", the `<code> <year>` pattern also matches "CBC, 2019", which overlaps both real citations without being contained in either, so it was reported as a third citation. It was reproduced on master for the stale and the invalid-year detectors, including the data-center modules. An overlapping match is now skipped. That removes only duplicate alerts, so no module's policy changes.
- **2026-09-24, S03 — California long forms: a little more than the four demonstrated.** Each demonstrated form is added, plus close relatives: `2022 Title 24` (the mirror of `Title 24, 2022`), `2022 California Green Building Standards Code` (CALGreen's formal name), and `CPC (2019)` (the parenthesized year without the word edition). All are in the California module only. `Title 24` is matched as itself, and the alert names only the year, so it is never equated with the CBC. The invalid-year detector reads these forms too, so `Title 24, 2024` is an invalid California cycle.
- **2026-09-24, S03 — ASCE also reads `SEI/ASCE 7-02`**, the form ASCE used for the 2002 edition. One old quirk is kept: a designation with no separator at all (`ASCE 716`) still reads as 7-16, as before.
- **2026-09-24, S03 — placeholder decisions.** `[OPTIONAL …]` and `[OPTIONS …]` stay placeholders along with `[OPTION …]`: in the supported templates a bracketed OPTIONAL is a keep-or-delete choice still to be made. `TBD-200` is an identifier, like `TBDF-200` and the `XXX-12` model number the template-marker rule already skips, so a TBD joined to a hyphenated or longer token never flags; `TBD - see drawings` and `TBD—by Architect` still do. A bare `tbd` in lower case flags too. The bare-TBD pattern is last in the list, so every older alert keeps its place.
- **2026-09-24, S03 — naming: "dominant" means a majority, and names without a section number stay out.** A style is the project's when more than half of the CSI-named files use it. Otherwise every CSI-named file gets a neutral "Mixed CSI filename styles (no dominant style)" alert with `dominant_style: None`. A name without a leading section number is neither counted nor flagged. Before, it was flagged as `found_style: "other"` whenever a recognized style dominated. The plan keeps the naming notice apart from coverage and routing, and a missing section number is a routing matter.
- **2026-09-24, S03 — the naming alert's `context` now says something.** It was just the file name, repeated under the same file name in the report. It now names the file's style and the project's: "23-31-13-Metal-Ducts.docx — dash-separated; most files are space-separated". That is the only change in the two preprocessor goldens (one line each, reviewed). Both exporters' section intro changed from "…differs from the project's dominant style" to "…differs from other files in the project" (`NAMING_ALERTS_DESCRIPTION`, shared), and the pipeline's preflight log line has a mixture wording.
- **2026-09-24, S03 — found, for S14:** the extractor's section attribution (`extractor._is_heading_paragraph`) is a separate heading heuristic, and it still treats a line like "1.5 inches minimum cover" as a heading. Its docstring calls such false positives harmless (they move a section boundary by one paragraph). S14 changes section attribution anyway and could reuse `heading_candidates`.
- **2026-09-24, S04 — one transaction model: rollback.** The plan allows either rolling back the unfinished part of a turn or answering it with an error `tool_result`. A turn (the question plus its tool rounds and `pause_turn` continuations) now commits only on `end_turn` or a stop sequence, and every other ending discards it whole, the question included. Two API rules decided it: a follow-up user message may hold only `tool_result` blocks (so an error result can't share a message with the reader's next question, and an unresolved server tool call would make that request fail), and a call whose input never arrived whole has no input to send back. The reader still sees everything that arrived, marked "Interrupted", and the question goes back into the message box.
- **2026-09-24, S04 — `max_tokens` and refusals roll back too.** Both are terminal stop reasons, but the answer is incomplete, and the API's guidance for a mid-stream refusal is to discard the partial output. So asking the chat to "continue" an answer cut off at the length limit no longer works; the notice says to ask a narrower question or choose a lower effort instead. That is user-visible, so it has a release-note line. `model_context_window_exceeded` and unknown stop reasons are handled the same way (the first as a notice, the second as an error), and `stop_sequence` commits like `end_turn`.
- **2026-09-24, S04 — invalid JSON is a stream failure; a missing required field is a model mistake.** The chat's tools don't use eager input streaming, so the API validates tool input before streaming it, and input that won't parse, or parses to something other than an object, means the stream itself can't be trusted: the turn fails and nothing runs. Input that parses but lacks a required field is answered with an error `tool_result` (`"Not run: missing required input …"`), the documented pattern that lets the model retry. Either way no tool runs on `{}` or on defaults.
- **2026-09-24, S04 — found and fixed: the chat's `pause_turn` continuation lacked the `container` id.** Its web search is `web_search_20260209`, whose dynamic filtering runs in a code-execution container. A pause while filtering needs the container named on the continuation, or the API rejects it, which is the failure CLAUDE.md ("Server-tool containers must survive a `pause_turn` resume") records for the main app. The chat now carries the last container id a response reported to every later request of the same turn, and never into a new turn. It was not in the plan's list, but WP-12 asks for valid continuations.
- **2026-09-24, S04 — forward compatibility.** Unknown event types and delta types are ignored, as the API's versioning policy asks; a known event that breaks the stream's contract (out of order, wrong block type, after `message_stop`) fails the turn. An event left without its terminating blank line at end-of-stream is not dispatched, per the SSE format, so a stream cut off inside its final `message_stop` frame is incomplete. The SDKs do the same.
- **2026-09-24, S04 — two small additions the plan implies.** Forget key and leaving the page (`pagehide`) also stop an answer in flight, since the next request of that turn would need the key. A stored model preference is now checked against the offered models: an older report's `claude-sonnet-4-6` in `sessionStorage` used to select nothing, and the request went out with an empty model.
- **2026-09-24, S04 — the probe became the harness.** `tests/fixtures/chat_key_probe.js` is deleted. The WP-12 check in `tests/test_plan_open_defects.py` now runs on the new harness, its strict-xfail marker is removed, and its control still passes. Three string pins in `tests/test_html_report_exporter.py` were updated for the new expressions (`serverToolsFor(turn.model)`, `effort: turn.effort`) and the new key copy; the behavior they stood in for is now asserted under Node.
- **2026-09-24, S04 — found in the harness: Node's `vm` swallows a throwing accessor on the sandbox object.** The first "storage unavailable" mode defined a throwing `sessionStorage` getter on the sandbox; Node's interceptor turned that into `undefined`, so it simulated *missing* storage, and a mutation that removed the chat's guard survived. The accessor is now installed from inside the context, the harness logs each denied access, and the test asserts the page actually hit it.
- **2026-09-24, S05 — one contract, two loops.** Both transports now classify a finished (non-paused) conversation through `verifier.classify_verification_turn` and build the result with `_stamp_verdict_result` or `_failure_result`; only pausing, retrying, and batching stay per transport. `VerificationResult.outcome` (runtime-only, never cached) names the kind on every result the verifier builds. Reproduced first on master `577f578`: text with no JSON after a searched turn was a grounded, cacheable UNVERIFIED in real time and a failure on batch; a verdict call with no `verdict` field or `"PROBABLY"` was a grounded, cacheable UNVERIFIED on both; a verdict call whose input was not an object was a clean UNVERIFIED in real time and a failure on batch; every real-time failure reported zero tokens.
- **2026-09-24, S05 — a turn with no search evidence is a failure on both transports.** The plan doesn't say how to classify it, and the transports disagreed: real time called it clean uncertainty (and escalated CRITICAL / HIGH findings to Opus), batch called it an operational failure. Batch's reading was kept — nothing was checked, so "insufficient evidence" would say something false — as `no_search` or `search_failed`. So real-time no longer escalates these (nor malformed replies); `should_escalate_verification` never escalates a failed pass, and the plan forbids adding a repair loop. The gate now counts search **and** fetch result blocks, as real time did: the batch gate also required a search and a non-zero `web_search_requests` counter, so a fetch-only conversation (which CLAUDE.md said passed on both) failed on batch. Blocks and counters agree in production; only fakes could tell them apart.
- **2026-09-24, S05 — what "malformed" means.** A verdict call whose input is not an object, a missing, null, or unknown `verdict` (no longer coerced to UNVERIFIED), several verdict calls that disagree, or text holding no valid verdict object. Case and whitespace in the verdict are forgiven, and the other fields stay tolerant, because each already has a safe defined outcome: missing sources demote through grounding, a missing quote demotes CONFIRMED / CORRECTED, and a missing explanation is just empty. A demoted verdict is still a well-formed verdict (genuine uncertainty), not a failure.
- **2026-09-24, S05 — incomplete stops are named.** `refusal` (with `stop_details`), `max_tokens`, `model_context_window_exceeded`, and anything else (`stop_sequence`, `None`, an unknown reason) each get their own outcome and explanation, and `retry_telemetry["terminal_reason"]` carries the outcome on both transports. `test_verification_stop_parity.py` pinned the old shared explanation and `terminal_reason: "terminal_unverified"`; it was updated on purpose and now also pins the outcome and the kept usage.
- **2026-09-24, S05 — two synthesized UNVERIFIEDs became failures.** The batch safety net ("No verification result after all batch waves.", reached when polling detaches or fails before the finding's wave finishes) and "No API key available" were clean INSUFFICIENT_EVIDENCE results; nothing was checked in either, so both are now VERIFICATION_FAILED (`no_result`, `no_api_key`). A finding left unresolved after the last wave with no failure class at all becomes `no_result` too; that path can't be reached today.
- **2026-09-24, S05 — usage is kept per conversation; cross-attempt accounting is for S09.** Every exit keeps the known usage of the conversation that produced it: each classified failure on both transports, a real-time exception after some responses, and the batch loop terminals (continuation cap, unresolved, tracker-terminated), which used to drop `accumulated_usage`. Not done, and recorded here for S09 (WP-15's attempt model): an attempt abandoned for a retry is not added to the final result. That covers a real-time retry after partial responses, a batch fresh retry after a paused conversation (its `prior_usage` is dropped when the retry context is built), and the real-time fallback after paid batch waves. Adding these into `call_usage` now would pre-empt S09's attempt identity.
- **2026-09-24, S05 — sharing uses a positive marker.** `_shareable_verdict` shares an UNVERIFIED only when the verifier stamped it `outcome == "verdict"` (and it isn't failed, budget-exhausted, or local). This fails safe: anything else, a synthesized terminal included, costs at most one extra attempt and is never an inherited non-answer. The budget terminals (`continuation_cap`, `search_ceiling`) are "budget failures" under the plan's exclusion even when `budget_exhausted` is False. The test doubles of a clean UNVERIFIED in `test_verification_singleflight.py` and `test_governing_basis_context_gate.py` declare `outcome=OUTCOME_VERDICT`, since `verify_finding` now stamps it.
- **2026-09-24, S05 — the cache predicate also refuses replays.** Besides the plan's list, a `hit` or `shared` result is never re-stored, because that would reset its age and pass a stale verdict off as fresh, and a local result is recognized by `verification_mode` as well as `cache_status`. Contested verdicts stay cacheable (they are grounded conclusions, and `models_disagreed` is persisted), and DISPUTED stays citation-gated but not quote-gated (the existing rule, pinned by a test). Two CLAUDE.md passages said the cache refused a quote-less DISPUTED. It never did, and both are corrected.
- **2026-09-24, S05 — what an invalid record is.** A creation time that is missing, a string or bool, not finite, not positive, or more than a day in the future (to allow clock skew between machines). A present `last_used_ts` that is invalid (absent or zero is a legacy row). A present persisted field of the wrong type, or a count that is negative, fractional, or not finite; a whole-number float such as `3.0` is fine. Found while reproducing: a single string timestamp raised out of `load_from_disk`, and the pipeline then started with an **empty** cache, so one hand-edited row threw away every valid one. The load now reports ignored rows in the run log.
- **2026-09-24, S05 — "substantive" uses `normalize_url`.** A source counts when `source_grounding.normalize_url` can reduce it to something, so punctuation-only entries such as `").."` don't count either, the same line the grounding validator already drew. A blank citation the model made still shows as a rejected `empty` citation (it is not dropped at parse time), and blank entries are dropped from every persisted source list.
- **2026-09-24, S05 — source-string pins became behavioral tests.** `test_budget_exhaustion.py::TestVerifierSourceInspection` (a local variable name, a helper signature) and `test_verification_failed_status.py::TestVerifierExceptionPathsMarkFailed` (it counted `failed=True` literals) both said the paths "can't be driven without a real API call". The scripted clients in the new `tests/fixtures/verification_drivers.py` drive them, now with real SDK exceptions.
- **2026-09-24, S05 — small consequences worth knowing.** The batch text fallback reads dict-shaped text blocks, as real time always did (a dict-shaped batch message with a text verdict used to read as "no content"). A shared follower's diagnostics event now records `api_call=False`, since the summary already skipped it. `_search_gate_failure` and `_collect_search_evidence` had no callers left and were removed. Handbook ch. 10 also said a grounding downgrade renders as DISPUTED and quoted `_CACHE_SCHEMA_VERSION = 3`; both were wrong and are fixed, since that table was being edited anyway.
- **2026-09-24, S06 — no strict xfails to remove.** S01's conversion of the old status script had no WP-08 check, so `tests/test_plan_open_defects.py` holds no S06 marker (19 xfails on 3.11 before and after). The defects were reproduced first on master with a scratch script (see the session log), and `tests/test_request_budget.py` holds the regression tests.
- **2026-09-24, S06 — one sizing rule per review spec; a padded guess never overrules an API estimate.** Master ran two gates: the count API for up to eight specs (else the top four), then a padded local gate over *every* spec, API-counted or not. WP-08 says padding applies when the count API is off or unavailable and that a padded count is never measured usage, so each spec is now judged once: on its API estimate when the preflight asked for one, on the padded local estimate otherwise. With the Opus padding corrected to 1.45×, keeping the second gate would refuse specs whose API estimate fits (roughly 448k–500k on Opus 5) on the strength of a guess the API had already answered. The error now names every oversized spec at once instead of stopping at the first.
- **2026-09-24, S06 — Opus 5 and Opus 4.8 pad by 1.45×, not 1.10×; unknown models by 1.50×.** Found while checking "model-specific conservative padding". The old comment reasoned that Opus 5 shares Opus 4.8's tokenizer (true) and so must not copy Sonnet 5's 1.45×. But that shared tokenizer is the one introduced with Opus 4.7, which Sonnet 5 also uses and which produces about 30% more tokens for the same text (Anthropic's token-counting guide; the models overview's ~555k words per 1M tokens on the current tokenizer against ~750k before it; the Sonnet 5 migration guide's "the same new tokenizer as Opus 4.7/4.8"). So the local fallback ran about 30% low for the default review model. The unknown-model default moved from 1.20× to 1.50× so it stays the widest margin, as its comment requires. The factors apply only when no API estimate exists; `tests/test_token_budgets.py`'s factor pins were updated on purpose.
- **2026-09-24, S06 — the ceiling and its reserve.** `input_ceiling = min(phase_limit, context_window − max_tokens − 5% of the window)`. The plan asks for "a documented reserve"; 5% reproduces the 50,000 tokens the 822k package limit already set aside on 1M-window models and gives 10,000 on a 200k window (Haiku 4.5: a 126,000 ceiling for a 64k-output request). The practical limits stay where they are more conservative: `RECOMMENDED_MAX` for a review, 822,000 for cross-check and compliance. The plan's numerical note says the matching local count is "about 566,897"; the exact largest fitting local count at 1.45× is 566,896 (822,000 / 1.45 = 566,896.55, and padded counts round up), and it is derived per request, not hard-coded.
- **2026-09-24, S06 — what the local fallback counts.** System, every message, each tool definition's JSON, `tool_choice`, and a flat 600-token allowance for the system prompt the API adds whenever tools are present (above the largest figure Anthropic's tool-use pricing table lists for the registered models); an API estimate already includes that prompt. `cache_control` markers are stripped from the counting form, so a TTL change cannot change a request's identity.
- **2026-09-24, S06 — the count cache moved and got stricter.** The old cache in `input/extraction_cache.py` keyed on hand-picked inputs (model, system, user message, project context, cycle label, tools) and missed `tool_choice` and `thinking`. It is replaced by a process-wide LRU (512 entries) in `core/request_budget.py` keyed by the SHA-256 of the complete counting form, model included; only valid API estimates are cached. `token_count_cache_key` and its helpers, `review_request_cache_key`, and `estimate_local_request_tokens` had no callers left and were removed.
- **2026-09-24, S06 — the batch builder reads the cache, never the network.** Item 10 asks to resolve the input count before choosing the output cap. The builder builds on the baseline cap, reads the preflight's cached estimate for that exact shape (else the padded local count), then chooses the cap; `review_request_budget` rechecks the fit against the chosen cap. Having the builder call the count API itself was rejected: it would double the count calls and bypass the preflight's top-K policy. So in a project of more than eight specs, a spec the preflight did not ask about decides extended output on its padded estimate, which errs toward the 300k allowance (a higher cap costs nothing unless the model writes that much, and it cannot push the ceiling below `RECOMMENDED_MAX` on a 1M window).
- **2026-09-24, S06 — how a group is split.** Specs keep their input order inside a CSI group, and each part is the largest contiguous run of the remaining specs whose own measured request fits, found by bisection (at most ⌈log₂ n⌉ + 1 counts per part). Only a run measured as fitting is accepted, so the plan stays safe even if counts are not monotone. A group that fits keeps its plain id, so a package that only needed CSI chunking plans exactly as before; parts are `<group>:<n>`, labelled "(part n of N)", and their findings keep the division's label, so report grouping is unchanged. Cross-check needs two specs per request, so a spec that fits alone but cannot be paired with a neighbor is not analyzed there (after the borrow step below); compliance runs such a spec on its own.
- **2026-09-24, S06 — a not-analyzed part is a skipped chunk.** No new chunk status was added (S07 owns completeness semantics). A part that cannot be sent becomes a `skipped` chunk result carrying its reason, so it counts in `chunk_skips` (the Run Diagnostics banner's "N chunk(s) not analyzed" flag) and its files are named in the summary. A whole-package request whose size cannot be determined at all is skipped with the reason rather than sent.
- **2026-09-24, S06 — package-pass counts are single-attempt.** The count client in cross-check and compliance is the no-retry view (`_get_client(sdk_retries=False)`); a failed count falls back to the padded estimate instead of retrying. The SDK would otherwise sleep through its backoff while holding a routed program's call permit, which item 9 forbids. The review preflight is not gated and keeps the SDK's retries, as before. Retry timing itself is S15's (WP-11).
- **2026-09-24, S06 — the real-time gate.** It reads `review_extended_output_count` for both the initial and the repair shape (the batch builder's basis). A spec that cannot be sized at all is refused with its own message; before, a tokenizer failure would have raised out of the gate.
- **2026-09-24, S06 — wording and names.** "Exact" for an API count is gone from code, logs, the GUI, and the docs: the gauge title reads "(API estimate)" or "(approx)", the log says "Token count (API estimate)", and the digest dialog says "(API estimate)" or "N of M request(s) counted by the API". `TokenGauge.update_gauge(is_exact=)` became `is_api_estimate=` (its one caller changed). Internal names (`refresh_exact_token_count`, `EXACT_TOKEN_REFRESH_DEBOUNCE_MS`, `DigestPreflight.exact`) were kept to avoid churn in GUI and test wiring; their docstrings say what they mean.
- **2026-09-24, S06 — the test suite could reach the live count endpoint.** Once budgets asked the count API, tests that built a real client with the placeholder key called the live endpoint, got 401s, and fell back, so they depended on the network without saying so. `tests/conftest.py` now blocks the SDK's `Messages.count_tokens` in every non-network test and clears the count cache per test; a test that wants an API estimate scripts one (`tests/fixtures/count_api.py`). Two network-only smokes (`test_cross_check_count_shape_smoke`, `test_compliance_count_shape_smoke`) send the exact counting forms, `tool_choice` and `thinking` included, to the live endpoint; they did not run here (no key), so the endpoint's acceptance of those two fields rests on the API documentation.
- **2026-09-24, S06 — docs fixed in passing.** CLAUDE.md's cross-check chunking paragraph named helpers that moved to `core/chunked_pass.py` (`_group_specs_by_chunk`, `_label_finding_with_chunk`, `_synthesize_chunk_findings`), and handbook ch. 12's "two minor hardening gaps" paragraph described a padding clamp and an extended-output guard that were both fixed in code long ago; both were corrected because they sit inside passages this chunk rewrote. Found and left for S18: handbook ch. 2 says `core` imports no other package at load time, but `core/chunked_pass.py` imports `review.reviewer` (new row in "Known-wrong statements").
- **2026-09-24, S06 — found in review (Codex, P2): a lone spec stranded at a part boundary.** Taking the largest fitting run each time can leave a run too short for the pass: four same-division specs of which any three fit became 3 + 1, and cross-check (two specs per request) reported the last one as not analyzed although 2 + 2 covers all four (reproduced first; the same happens inside a group when a lone spec cannot pair with the spec after it). Such a run now borrows the fewest specs it needs from the end of the part before it (`chunked_pass._borrow_from_previous`), and only when both new parts are measured to fit and the lender keeps enough specs, so no spec the plan already covered is given up. This is a local repair, not an optimal partition: a lone spec whose previous part has exactly two specs, or whose pair does not fit, is still reported. Borrowing more than the minimum cannot help (the run did not fit with its next neighbor, so it will not fit with more), and a full repartition would cost count calls the plan bounds. Compliance needs one spec per request and never borrows. The not-analyzed reason now says the spec "could not be paired with a neighboring specification" instead of "fits only on its own", which was untrue for a spec that fits with a neighbor already used by another part.
- **2026-09-24, S07 — no strict xfails to remove.** S01's conversion of the old status script had no WP-09 check, so `tests/test_plan_open_defects.py` holds no S07 marker (19 xfails on 3.11 before and after). The defects were reproduced first on master with a scratch script (see the session log), and `tests/test_compliance_completeness.py` holds the regression tests.
- **2026-09-24, S07 — where the contract lives.** `src/compliance/completeness.py` is stdlib-only: `AssessmentUnit`, `reconcile`, `CoverageCompleteness`, `combine`, and the gap phrases. The checker, its chunked merge, the program merge, and both exporters share it. `ReviewResult` gains one additive field, `coverage_completeness` (`None` on review and cross-check results); nothing is persisted, since compliance results never enter saved state.
- **2026-09-24, S07 — a synthetic row is `unclear`, told apart by `origin`.** The plan asks for a synthetic "unclear / not assessed" row. The status stays in the closed four-value set, so older consumers read every synthetic row as `unclear`, the safe reading, and `origin: "synthetic"` plus `assessment` (`none` = no request returned the id, `partial` = returned somewhere but not established package-wide) tell it from the model's own `unclear`. Both reports label them NOT ASSESSED / NOT FULLY ASSESSED. Rows come out in expected-set order, so a reordered response gives an identical result.
- **2026-09-24, S07 — "without discarding their locations".** Every returned row that carries a location and loses the precedence merge is kept on the winner's `also_reported` list (status, evidence, file), and the evidence cell renders it ("Also reported represented: … — a.docx"). The D-7 precedence itself is unchanged; this also applies to duplicate rows within one response, which used to lose the losing row's location.
- **2026-09-24, S07 — zero controlling requirements is `completed`, not `skipped`.** The plan asks for "a valid no-applicable-items result". The old red `skipped` status said the specs were never evaluated, and a program's worst-status roll-up turned it into a program-level `skipped`. It is now `completed` with no API call, `state: no_applicable_items`, and a summary naming the unverified and process items that exist; the banner reads "no applicable requirements (none grounded)", not red. A missing profile (a recovery without saved state) stays `skipped`, and its expected set is recorded as unknown (`None`), never as "no applicable items".
- **2026-09-24, S07 — "every relevant chunk" means every chunk, and failed-review specs count.** Which chunks are relevant to a requirement cannot be decided deterministically, so every planned chunk is treated as relevant: `missing` stands only when every chunk returned it and nothing went unassessed. Beyond the plan's "failed, skipped, or omitted chunk", a specification excluded before the pass because its own review failed (`truncated_specs`) is also unassessed scope: `run_compliance_for_batch` passes those as `excluded_specs`. Before, a single pass over the remaining specs could call a requirement missing from the package although the one spec that might hold it was never read.
- **2026-09-24, S07 — which additions are held.** Every compliance ADD rests on its requirement being absent, so an ADD is executable only when a controlling requirement it references is an established `missing`. Held (report-only, never dropped): a referenced requirement not assessed, not assessed everywhere, or `unclear` somewhere; in a single pass, a requirement the same response called `represented` or `contradicted` (this used to stay an edit; the chunked path already dropped it as disproven, but a single pass has no other chunk to prove anything, so it is held, not dropped); an ADD resting only on an unverified item or a process advisory (the prompt already forbade those, now the code enforces it); and an ADD naming no requirement while some specification went unassessed. Kept as before: the chunked D-7 drop of a chunk-local ADD disproven elsewhere, and the chunked one-ADD-per-requirement dedup. Master dropped a chunk-local ADD whenever the merged status was not `missing`, so an `unclear` from another chunk silently erased it; that is now a hold.
- **2026-09-24, S07 — a hold keeps the proposed text.** `_demote_to_report_only` clears the edit slots, which is what guarantees a held addition never reaches the sidecar. To keep it usable as a conditional, the reason quotes the would-be insertion and its anchor. Both reports render a hold as "Addition held as REPORT_ONLY and not emitted as an edit. <reason>" (`is_held_addition`, keyed on the reason's `HELD_ADDITION_REASON_PREFIX`), not as "demoted at parse time". The banner's demotion row still counts holds under its "at parse time" label; that label was already wrong for anchor-validation demotions, so it is a new Known-wrong row for S18 rather than a wording change here.
- **2026-09-24, S07 — the engine hook.** `run_chunked_pass`'s compliance-only `coverage_merge` (which received only completed chunks' coverage — the root of the failed-chunk defect) and `finding_filter` were replaced by one `finalize(combined, outcomes)` hook that receives a `ChunkOutcome` for every planned chunk. It runs after the engine sets the execution status and the all-failed error, so the pass decides what the output means and never whether a chunk ran. Cross-check passes no hook, as before. `test_chunked_pass_engine.py`'s hook tests were rewritten on purpose.
- **2026-09-24, S07 — missing metadata is never complete.** `run_compliance_for_batch` stamps a record on every result it stores, its three skip paths included, and runs `ensure_coverage_completeness` on whatever the checker returned, so a stand-in pass (the data-center end-to-end test patches one in) is reconciled with the single-pass rules. A hand-built result with no record renders "coverage completeness not recorded" (red) and a matching note, and a program with one such module is `unknown`.
- **2026-09-24, S07 — program roll-up.** The program banner sums gap counts only over modules whose pass completed: a skipped or failed module already shows its own red hint, and adding its whole expected set as "not assessed" would restate it. A module's unassessed specifications keep its label ("Fire Suppression: 21 13 16 Dry.docx"), as failed-review specs do.
- **2026-09-24, S07 — small consequences worth knowing.** The coverage schema description changed, so the compliance tool definition's bytes changed (one prompt-cache write for that prefix, then warm again). The compliance trace span gains a `coverage_state` output. The GUI's terminal button state is unchanged (it is keyed on review errors); the completeness warning shows in the GUI log through the pipeline's log line. The About dialog's compliance paragraph now describes NOT ASSESSED and held additions. The recovery CLI exports DOCX and sidecars through the same exporters, so it carries the same notice.
- **2026-09-24, S07 — found in review (Codex, P2): a skip that assessed nothing must name what it did not assess.** When a recovery had a saved requirements profile but no re-extracted spec content, the "content not available" skip recorded every requirement as omitted and left `unassessed_specs` empty, so the sidecar, `.profile.json`, diagnostics, and program roll-up could not say which specifications went unassessed although the run's `files_reviewed` still named them (reproduced first: two new tests failed on the branch). The profile-unavailable skip had the same gap and was fixed with it: both now name the run's reviewed files (`pipeline._package_file_names`). The banner is unchanged, since a skipped pass shows its own hint and reports no gap counts.
- **2026-09-24, S08 — no strict xfails to remove.** `tests/test_plan_open_defects.py` holds no WP-14 marker (19 xfails on 3.11 before and after). The defects were reproduced first on master with a scratch script (see the session log), and `tests/test_repair_recovery.py` and `tests/test_repair_recovery_gui.py` hold the regression tests.
- **2026-09-24, S08 — where the contract lives, and what is persisted.** `src/orchestration/collection_outcome.py` is stdlib-only, like S07's `completeness.py`: `RepairOutcome`, `CollectionOutcome`, the stage names, the provisional wording, and `decide_saved_state_cleanup` with its identity helpers. The outcome is in memory only (`CollectedBatchState` → `PipelineResult`); nothing new is saved, because the saved record already carries the one thing a later collection needs, the repair batch id and request map. No schema changed: not the pending state, not the sidecar (additive keys), not the cache.
- **2026-09-24, S08 — "unknown is not finished" includes a failed read of a repair that exists.** Once a repair batch has an id it is billed and may still finish, so every later failure — polling that detaches (`pending`), polling that fails, or an exception while retrieving (`unreachable`) — keeps the saved record and makes the run provisional. On master the retrieval exception logged "will appear as failed" and the GUI then cleared the record. Only a repair that could not be submitted at all is `not_submitted`, and a saved repair that ended unusable with no replacement possible is `unusable`; neither is outstanding.
- **2026-09-24, S08 — deferral, not stage checkpoints.** The plan prefers the simplest reliable policy, and a repaired review changes the inputs of every later stage (the dedup groups, cross-check's already-identified list, compliance's excluded specs), so a provisional run runs none of verification, cross-check, compliance, or drawing impact, and never opens the verification cache. They run once, on the next collection. The cost: a provisional report has no verified findings at all, even for the specs whose review was fine, and it says so ("None of the findings below has been verified").
- **2026-09-24, S08 — a routed program waits as a whole.** Collection now has two phases: every module's review results and repair, then — only when no module's repair is outstanding — every module's dependent stages. Letting the settled modules go ahead would pay for their verification now and again on the next collection, because a program is re-collected as a whole. The program's drawing-impact pass is deferred too (`deferred_program_stages`). A module that cannot be collected at all is still a module error and holds no one back.
- **2026-09-24, S08 — one rule, and the cases it changes.** Keep on a module error, a missing outcome, an outstanding repair, or when every submitted spec failed; clear otherwise, a valid zero-findings run included; `--keep-state` keeps a complete run; a real-time run is outside the rule. Behavior that changed: the GUI no longer clears an all-failed run (it now matches the CLI), and the CLI's exit code and decision no longer look at the primary batch's terminal status — an expired or canceled batch's unfinished items go through the repair pass, whose outcome the rule reads — so a batch that ended that way and produced usable reviews now exits 0 and clears, as it always did in the GUI. The CLI exits 0 exactly when the run is complete.
- **2026-09-24, S08 — identity before clearing.** A record is cleared only when every primary batch id it names is one of the run's and any repair id it names is one the run settled (consumed, or replaced). An unreadable or newer-schema record is never deleted, and the resume prompt's No deletes only the record it showed. Master's GUI deleted whatever was on disk after any successful collection, so recovering batch X by id deleted batch Y's record (probe B). Recovering one child of a saved program, in the GUI or the CLI, now leaves the manifest in place for the other children.
- **2026-09-24, S08 — an in-process lock, not a file lock.** Probe F reproduced the lost update: two program children stamping their repairs at once each loaded the manifest, and the second save erased the first child's stamp. Every read-modify-write of the pending file now takes `batch_resume._STATE_LOCK` (an `RLock`, since a stamp saves through the locked writer). The GUI and the CLI running at the same time are two processes and are not coordinated; the identity check keeps either from deleting the other's record, and a simultaneous write by both was already possible on master.
- **2026-09-24, S08 — re-attaching moved before the source-file check.** On master, a collection with no extracted specs (source files moved, or a bare-id recovery with no input folder) skipped the repair pass before looking at the saved repair, so a saved, billed repair was never collected (probe G). Re-attaching now reads the file names from the primary request map and runs first; only a fresh repair submission needs the specs.
- **2026-09-24, S08 — a provisional bare-id recovery saves a record (adoption).** Beyond the plan's list: a run recovered by batch id alone has no record, so if its repair were still running, the id would live only in the log and a second recovery by id would submit a second repair. `adopt_outstanding_run` saves a record carrying the repair id — only when the state file is free, never over another run's record (it logs both ids instead) — and, on a run that already has its record, re-stamps a repair id whose earlier save failed. Used by the GUI's collection and the CLI.
- **2026-09-24, S08 — Recover batch… resumes a saved id from its record.** Found while writing the parity tests: given the id of a batch the app had saved, the GUI dialog rebuilt a thin submission by id, which knows neither the module nor the repair, and so would have submitted a second repair. It now routes such an id (or a saved program's child) to `start_batch_resume`, as the CLI already did. The startup resume prompt also says when a repair batch is recorded.
- **2026-09-24, S08 — how a pending spec reads in the report.** A spec whose repair is still outstanding counts as failed review in the banner row and the Files Reviewed list (as of that report it was not reviewed), but the "re-run this spec" hint leaves it out and its review error says to collect the run again; re-running it separately would pay for its review a third time. The provisional notice is the first hint of all.
- **2026-09-24, S08 — the applier does not read `provisional`.** The sidecar marks a provisional run, but `applier/` was not changed (its contract is to consume what the sidecar already emits). Every finding in a provisional run is unverified (`NOT_CHECKED`), which the `strict` and `conservative` policies already hold; only `--policy all` would apply one. A candidate for a later chunk, not taken here.
- **2026-09-24, S08 — accounting is S09's.** A later collection re-reads the primary batch's results, and the collection event records their usage again in that session's diagnostics (as on master); a merged repair still replaces the primary attempt's usage. Both are WP-15's attempt model (S09): the collection outcome gives it the repair batch id and whether this collection submitted it (`RepairOutcome.submitted`) or re-attached to it.
- **2026-09-24, S08 — not done: a later-phase module error still repeats siblings on resume.** If a module fails in the second phase, after its siblings' verification ran, the record is kept (a module error) and resuming re-runs every module's dependent stages, as on master; the verification cache replays conclusive verdicts for free, but cross-check and compliance are paid again. Avoiding that needs per-module completed-stage checkpoints with input fingerprints, which the plan says not to add casually.
- **2026-09-24, S08 — test harness notes.** `gui.batch_controller` binds `verify_findings_for_run` at import, so the GUI tests patch that module attribute; without it one test reached the live API with the placeholder key (a 401). A guard on the SDK's HTTP transport confirmed the S08 tests make no HTTP call. `clear_pending_batch` is kept as an unconditional utility (its own tests use it); production code no longer calls it. Updated on purpose: `test_review_repair_hardening.py` (the repair pass returns an outcome; re-attach returns a disposition with its detail and treats poll failure as unreachable), `test_program_pipeline.py` (a phase-one stub), `test_recover_in_progress.py` (stub results carry settled outcomes, since a result with no outcome never clears), and `test_deterministic_checks.py` (the repair stub's return shape).
- **2026-09-24, S08 — found in review (Codex, P1): a kept record must name every repair the run created.** A repair's id is stamped onto the saved record when the repair is submitted, but that write can fail after its retries (a file briefly held by a scanner), leaving the id only on the in-memory submission. The CLI re-stamped only for a run recovered by id alone, so a saved run kept its unstamped record and the next run of the tool paid for a second repair (reproduced first: the saved record had no repair id after the first run, and the second run submitted another repair). The GUI's single-module path already re-stamped, but its program path did not, and neither did the CLI's, so a program child had the same gap (reproduced first too). The fix is in the one shared step, `apply_saved_state_cleanup`, which both drivers now give the run's `submission`: when it keeps a record, a provisional single-module run goes through `adopt_outstanding_run` as before, and every other kept run through the new `restamp_repair_batches`, which writes any missing repair id onto the run's own record, for each program child too, never onto another run's record and never by creating one. That covers the non-provisional cases the finding did not name: a program kept because a module failed, or a run kept because every spec failed, whose consumed repair would otherwise be paid for again on resume. The CLI now passes the saved record's own `input_dir` / `files` for the case where the record has to be written again; `_build_submission` returns the submission alone, since the saved-state flag it also returned no longer decided anything.
- **2026-09-24, S09 — no strict xfails to remove.** `tests/test_plan_open_defects.py` holds no WP-15 marker (19 xfails on 3.11 before and after). The defects were reproduced first on master with a scratch script (see the session log), and `tests/test_attempt_accounting.py` and `tests/test_attempt_accounting_gui.py` hold the regression tests.
- **2026-09-24, S09 — where the contract lives.** `src/core/attempt_usage.py` depends only on the stdlib and `core.api_config`, because the reviewer, the real-time runner, the verifier, triage, the pipeline, and diagnostics all build records and none of them may import another's layer for it. The records are runtime telemetry: nothing new is persisted, no schema moved (pending state, sidecar, cache), and `VerificationResult.transport` joins `call_usage` in the cache's `_SKIPPED_FIELDS`.
- **2026-09-24, S09 — identity.** The plan's "batch ID plus item/custom ID and attempt role" for a batch item; a synchronous request is identified by its response's message id (`ReviewResult.message_id` is new; a real-time verification conversation takes its first response's id); a request that raised has no identity and is recorded exactly once, by the loop that made it. A multi-wave batch verification conversation is one attempt, identified by the wave item that last reported its usage, because the wave loop accumulates usage per conversation rather than per item. `tests/fixtures/fake_anthropic.FakeMessage` now gives every instance its own id (they all shared `msg_fake_1`, which would have collapsed distinct attempts).
- **2026-09-24, S09 — known, unknown, unbilled.** An errored, canceled, or expired batch item is a known zero: the Message Batches API does not bill them. A synchronous request that raised is *unknown*, not assumed unbilled — an HTTP error before any response is very likely free, but "likely" is not known, and the plan says not to invent a zero. Unknown attempts are counted (by operation and phase), named in every surface, and never priced.
- **2026-09-24, S09 — scope, and what "earlier" means.** `earlier` is spend billed before this collection started: the primary batch of a submission rebuilt by `reconstruct_batch_submission` (both the saved-state resume and bare-id recovery go through it; `BatchSubmission.resumed`, in memory only), and a repair batch an earlier collection submitted that this one re-attached to. The GUI starts a new diagnostics report for every run and every resume, so a report's `earlier` line is always relative to its own collection. Verification and every later pass are always this run's.
- **2026-09-24, S09 — aggregates stay aggregates.** The passes that already record one summed total per call site — a research dimension, a drawing-digest chunk, cross-check, compliance, drawing impact — keep recording it, now labelled with its `operation` (priced as basis `aggregate`). The plan allows "an already aggregated total"; per-call identity for them served no acceptance criterion. Cross-check's one real gap was fixed instead: its parse-error re-request overwrote the first response's usage, and now sums it (probe F1).
- **2026-09-24, S09 — beyond review and repair.** WP-15's title is review and repair, but S05 explicitly deferred cross-attempt verification accounting here ("that is plan WP-15's attempt model (S09)"), and the chunk is "every paid attempt", so the same model covers: a real-time verification retry keeps the conversation it abandoned (E1); a batch fresh retry keeps the paused waves it restarted from (E2); a real-time fallback keeps the paid batch waves before it (E3) and is priced at standard rates instead of the batch discount (E4); the detach safety net keeps the waves it read plus an unknown record for the wave in flight; an escalation batch submitted but never read adds an unknown escalation record; a real-time worker crash stamps one unknown record; a real-time review call that raised gets its own row with unknown usage (G1, G2 — before, only the terminal failure got a row, of zeros). Haiku triage was never recorded at all; `verify_findings_for_run` and everything down to `triage._classify_batch` take an optional `usage_sink`, and both drivers pass `diagnostics.triage_usage_sink` in both rounds.
- **2026-09-24, S09 — found while reproducing: evicted events took their spend with them (H1).** `summary()` priced the estimate by walking the event list, which keeps the newest 5,000 events; a long batch poll's progress lines evicted the oldest, usually the review batch, and its spend left the estimate with it. The billing input of every API-call event is now taken before the caps and kept in a ledger that is never evicted (`DiagnosticsReport._billing`), so neither the event cap nor the per-event byte cap can touch what is priced. (A second probe, a per-call list pushed past the byte cap, priced correctly on master too; it stays as a test of the ledger, not as a defect.) `tests/test_diagnostics_concurrency.py` pinned the old behavior (totals equal to the *retained* events) and was updated on purpose.
- **2026-09-24, S09 — the diagnostics shape, and the wording.** A verification event now carries its records as `attempts` (dicts with operation, role, transport, identity) instead of `call_usage`; the flat fields are unchanged. `cost_summary` gains `estimate_note`, `by_scope`, `by_category`, `unknown_usage_attempts`, `duplicate_attempts_ignored`, and `legacy_records`, and each phase gains `unknown_usage_calls`; nothing was removed. The text export's "Est. Cost (USD)" line became `cost_summary_lines`' "Estimated cost (USD): $X — an estimate from the recorded usage at list prices, not an invoice" plus detail lines, and the recovery CLI's "Accounted cost" line became the same lines plus "Not in the estimate: the original run's location research and any drawing digest". Updated on purpose: `test_diagnostics_cost_pricing.py` (the wording), `test_verification_token_telemetry.py` (the event's `attempts`, and a single-call wave result now carries its record), `test_batch_escalation.py` (the entries are attempt records), `test_diagnostics_concurrency.py` (above), and `test_recover_in_progress.py` (the CLI's lines).
- **2026-09-24, S09 — the GUI Diagnostics window shows the estimate.** It showed no cost figure before (only the text and JSON exports carried one); it now renders the same `cost_summary_lines` in an "Estimated Cost" block. Tested headlessly with fake widget classes on 3.12 with Tk.
- **2026-09-24, S10 — nine strict xfails removed.** All nine `fixed by S10 (WP-02)` reproductions in `tests/test_plan_open_defects.py` turned `[XPASS(strict)]`, and their markers were removed; they stay as regression tests. The 3.11 xfail count went from 19 to 10.
- **2026-09-24, S10 — one walk, shared with the writer.** The extractor and the applier's writer both read a paragraph through `_paragraph_segments`: each segment carries its text, its run, and its route (the containers between the paragraph and the run). The writer counts occurrences and checks where a match came from over exactly the text the review saw, so the two cannot read one paragraph differently. `_direct_span` also refuses if a run's own text ever disagrees with its segment. A paragraph with no wrappers reads byte-identically to python-docx's `Paragraph.text` (pinned).
- **2026-09-24, S10 — field state is per paragraph.** `_FieldState` follows begin / separate / end within one paragraph. A field that spans paragraphs (a table of contents inserted as a bare field, with no control) leaves its later paragraphs readable as ordinary text, as on master; its first paragraph's instruction is still never read. Carrying the state across paragraphs would change how every paragraph after an unbalanced field reads, and no acceptance case needed it.
- **2026-09-24, S10 — the `cc` id namespace.** A `cc<n>` step's `n` is the control's physical child index in its container (the body, a `w:tbl`, a header, a text box, a note), the same way `pN` is physical; a control nested in another adds `cc<i>`, its index in the outer control's content. So an id says where the control sits, and adding a control renumbers nothing outside it. The legacy grammar is unchanged. The contract pins' id grammar was widened on purpose, and every `cc` id is checked against an independent reading of the XML.
- **2026-09-24, S10 — content controls are never written.** The plan allows either deterministic resolution or explicit unsupported classification; S10 chose unsupported. A control can be locked (`w:lock`), bound to a custom XML data part that Word rewrites it from (`w:dataBinding`), or showing its placeholder text (`w:showingPlcHdr`), and a tracked change inside one would need rules the plan doesn't ask for. `ElementKind.CONTENT_CONTROL` gives the refusal its own reason ("apply this edit by hand").
- **2026-09-24, S10 — controls inside tables.** A control around paragraphs in a cell, or around a whole cell (`w:tr/w:sdt`), adds its text to the row in document order, and the row keeps its legacy id (it is still one `w:tr`). A control around whole rows (`w:tbl/w:sdt`, a repeating section) gets rows of its own, `<table>cc<k>r<i>`, so the table's own rows keep their numbers. The applier resolves a legacy row through the same `_row_text_cells` list and refuses any match in its wrapped part. A table inside any of those controls is not read, and is warned.
- **2026-09-24, S10 — the table of contents is skipped.** A control in the "Table of Contents" document-part gallery (what Word's References → Table of Contents inserts) is skipped: it is a generated copy of the headings, which are read where they stand. Evidence: reading it on the clean three-PART fixture gave 10 false structure alerts (3 empty-section, 7 duplicate-heading), and it would put every heading into the review prompt twice. Master never read the control at all, so this keeps master's behavior for it. A bare-field table of contents (no control) is read, as on master; recognizing its `TOC` instruction is a possible follow-up. Controls from other galleries are read.
- **2026-09-24, S10 — found: each text box was read twice.** Word saves every DrawingML text box together with a VML copy in `mc:AlternateContent`, and the extractor read both, so each text box in a Word-saved document reached the review twice. `_text_boxes` now reads, in each `mc:AlternateContent`, the first branch that holds a text box. As a result, `tb<b>` numbers in Word-saved documents change, because the VML copies no longer take a number. The applier never writes text boxes, so no instruction changes meaning; a `tb` id is reported as unsupported, as before.
- **2026-09-24, S10 — which unread structures warn.** Requirement 5 ("warn on unsupported text-bearing structures that remain omitted") is applied to the four kinds the new walk meets and cannot read: equations, embedded documents, legacy drop-down entries (Word records the choice as a position in the field's list, not as text in the paragraph; looking the entry up by that position is a possible follow-up), and tables inside table-level controls. The gaps that predate S10 (SmartArt, tables and text boxes in headers and footers, and tables in text boxes and notes) are listed in CLAUDE.md but don't warn. Detecting them means walking surfaces this chunk otherwise leaves alone, and a header or footer laid out as a table (project name, section number, page) would put the warning on every document built from that template, although that text is rarely a requirement. Reading those surfaces is the better follow-up.
- **2026-09-24, S10 — the editable copy no longer wins.** Before S10, when the target text sat in a body paragraph and also in a text box or note, the locator filtered its matches to editable elements and resolved the one left (`test_an_unsupported_container_never_wins_over_a_writable_one` pinned it). WP-02's last acceptance case (identical text inside and outside a control must never send an edit to the wrong occurrence) makes that unsafe, because the finding may have meant the other copy. Now any copy in an element the applier doesn't write makes an id-less match ambiguous, unless the finding's section singles out one copy; if that copy isn't editable, the edit is refused. The rule covers text boxes and notes as well as controls, since the reasoning is the same. The old test was replaced on purpose by three that pin the new rule.
- **2026-09-24, S10 — what the writer refuses, checked on master.** The writer refuses a match containing any character reached through a wrapper, with that wrapper's reason (content control, field result, hyperlink, smart tag, custom XML, text direction, another author's insertion), and a span that isn't made of adjacent plain runs: a field character, control, tracked deletion, or anchored object between them. Bookmarks, proofing marks, and permission and comment ranges inside a span are still fine. Checked on master before recording this: an edit across another author's deletion applied, and Reject All then read "Pipe shall be steelcopper ." instead of "Pipe shall be copper steel."; an edit reaching into a cross-reference's result moved "23 05" out of the field, so a field update would print it twice; and one reaching out of a result moved " for" into the field, so an update would drop it.
- **2026-09-24, S10 — additions beside wrapped text.** An ADD's anchor may sit beside a wrapper but not inside one. An ADD with no anchor text, positioned by a paragraph inside a block control, is refused.
- **2026-09-24, S10 — the writer reads layout characters the way the extractor does.** `_run_text` now renders tabs, breaks, and non-breaking hyphens as python-docx does, so the writer's offsets match the extractor's. A run carrying them is still never split (unchanged refusal).
- **2026-09-24, S10 — S01's hyperlink finding is fixed.** An edit aimed at hyperlink text is now refused with "the target text sits inside a hyperlink…" instead of the tracked-revision message.
- **2026-09-24, S10 — `prompt_serialization.py` unchanged.** WP-02 lists it as a target, but the new ids render through its existing element-type rules (`<para>`, `<heading>`, `<row>`), pinned in `tests/test_extraction_content_controls.py`.
- **2026-09-24, S10 — found: grouped-shape text boxes were already read.** TRUST_AUDIT's "grouped-shape text" gap, repeated in CLAUDE.md and the handbook, overstated it: a text box inside a grouped shape is read, on master too (checked with a probe there). The CLAUDE.md gap list and the handbook's new currency note now say so, and `test_text_boxes_inside_a_grouped_shape_are_read` pins it. SmartArt, whose text lives in a separate diagram part, is the gap that remains. The older handbook narrative (~lines 531, 605, 615) is left as captured; the currency note points at it.
- **2026-09-24, S10 — found in review (Codex, P1): equations at block level.** A display equation can stand beside paragraphs (`m:oMathPara` as a child of the body, a block control, a table cell, or a header, footer, text box, or note), where the paragraph walk never meets it, so it was left out of the text without the equation warning. Every block-level walk (the body loop, block-control content, `_cell_paragraphs`, `_story_paragraphs`) now records it in `_Unsupported`, and `tests/test_extraction_content_controls.py` covers nine placements. Mutation-checked: removing any of the nine new recording branches or arguments turns a test red.
- **2026-09-24, S10 — fixtures added.** `build_table_controls_spec`, `build_nested_controls_spec`, `build_table_of_contents_spec`, `table_of_contents_control`, and their sentinels, with shape pins in `tests/test_spec_docx_fixtures.py`.
- **2026-09-24, S11 — no strict xfail named S11; two added for S12.** S01 gave the one WP-06B reproduction to S12 because it drives the sidecar writer, which S11 leaves on schemas 4 and 5; it still xfails. The consumer audit found a defect for S12 (below), recorded as two new strict xfails with controls. The 3.11 xfail count went from 10 to 12.
- **2026-09-24, S11 — "validated" means checked against the text the review read.** Nothing validated the review model's `evidenceElementId` before. An element now counts as `validated` when it exists in that file's paragraph map and contains the edit's locator text (EDIT/DELETE: `existingText`; ADD: `anchorText`), with the applier's own test: exact, or whitespace-collapsed, never case-tolerant. Without the text (a recovery whose files moved, or a caller that passes no index), a named element is `claimed` and still separates occurrences: the applier confirms every id against the document itself, and its duplicate detection keeps a wrong claim from writing twice. S12's writer should pass `element_index_from_specs(pipeline_result.extracted_specs)`.
- **2026-09-24, S11 — an uncertain member stays its own occurrence.** A member naming no usable element, beside members that do, is neither folded into a located occurrence (that would assume identical prose means an identical place, which the plan rules out) nor made a second located one. It is one uncertain occurrence per file and instruction: the applier finds its text if it is unique, writes it once if that lands on an occurrence already applied (`DUPLICATE`), and refuses it as ambiguous otherwise, which keeps a second place the review failed to name in front of a person instead of dropping it, as the per-file model did.
- **2026-09-24, S11 — the occurrence model mints no finding identity.** The first draft computed a finding id for a finding that had none; S02's AST tripwire failed it, rightly, since identity may come only from the run's context. The finding id is used as the run stamped it, and a finding never given one (never the case in production) groups under `""`.
- **2026-09-24, S11 — missing originals borrow nothing.** A listed file with no recorded original gets the group's shared edit text with no element and no anchor, so such an ADD has no place and the reader says so in its own words. `FindingOccurrence.executable_finding()` keeps its old display fallback to the representative; it has no production consumer, and `test_dedup_edit_identity` pins it.
- **2026-09-24, S11 — for S12: content twins.** Two identical coordination findings share a `cf-` id by design, so their occurrences share an id. `edit_occurrences` lists one and keeps the other finding in `also_reported_by`, with its own verification, because the unique-key rule forbids two entries and position must not choose. S12's writer should give the entry the least trusted report status among them.
- **2026-09-24, S11 — the writer stays byte-identical.** `edit_sidecar` no longer goes through `group_findings` for schemas 4 and 5; the pre-S11 expansion lives on verbatim as `_legacy_file_occurrences`, and a new golden, `tests/goldens/legacy_sidecar_payloads.json`, captured by running 12 scenarios on master, pins it byte for byte — including the two behaviors S12 retires (one entry per file, and a borrowed locator under `has_per_file_original=False`). S12 changes that golden on purpose.
- **2026-09-24, S11 — the 6/7 contract, defined from the reader's side.** The plan asks for `occurrence_id` and a documented unique key including module provenance. Added: a required `location_basis` (`validated` / `claimed` must name an element; `unresolved` / `missing_original` must not, so a locator never borrows another place's element), `module_id` required in 7, and `has_per_file_original` dropped from 6/7 entries (the reader derives it from the basis). A repeated `(module_id, occurrence_id)` refuses every copy as malformed: the file broke its own contract, and keeping one copy would let position choose. Schemas 4 and 5 keep their key, their `has_per_file_original`, and their tolerance of repeated keys (content twins); an `occurrence_id` key in them is ignored. `PROGRAM_SCHEMA_VERSION` stays 5 for callers that named it.
- **2026-09-24, S11 — holding conflicts meant planning every edit first.** Resolving elements before writing (which the applier did) is not enough to tell what two edits change, and each edit found its text again after the earlier ones had changed the paragraph. Reproduced first: overlapping edits — the first listed won and the second "could not be found" (C1); an identical edit listed twice read as a failure (C2); two additions at one anchor landed in list order (C3); and two *compatible* edits blocked each other depending on order, when one's replacement repeated the other's target (C4) or changed an addition's anchor (C5). `DocumentEditor.plan` now fixes each edit's span or insertion gap, and raises every writer refusal, before anything is written, and `apply_planned` writes from those positions. That puts `applier/docx_edit.py` and a new `applier/conflicts.py` in this chunk, beyond the files the plan lists.
- **2026-09-24, S11 — what counts as the same place.** EDIT and DELETE: spans in one paragraph that share a character (spans that only touch are compatible). ADD: the same gap between siblings — after one paragraph and before the next is one gap. Identical means the same action, span or gap, and exact text, and is written once; anything else at one place is an `EDIT_CONFLICT` for every instruction involved, a chain of overlaps included. Two different additions at one place are held rather than ordered by a rule, because nothing in the sidecar says which comes first and the applier refuses rather than guesses. An addition and an edit to its anchor's text are compatible: the addition's place is found before any edit runs.
- **2026-09-24, S11 — two new outcomes, additive in the receipt.** `EDIT_CONFLICT` needs a person (the attention list; `--strict` counts it as unapplied). `DUPLICATE` names the instruction written in its place, takes that instruction's failure if it fails, and turns to `FAILED` with it if the copy is never saved; `--strict` does not count it. Receipt outcomes gain `occurrence_id`, `location_basis`, and `related_entries` (null and `[]` for schemas 4 and 5), and `by_outcome` gains the two statuses. Nothing is removed.
- **2026-09-24, S11 — "legacy behaves exactly as before" is about reading.** The checklist line sits under the reader item, and it holds there: legacy entries, keys, fields, malformed rules, and `is_program` are unchanged (pinned field by field). What legacy edits do to each other is now settled before writing, because the conflict item applies to every schema: a legacy sidecar with overlapping or repeated instructions, or with edits that blocked each other by order, gets `EDIT_CONFLICT`, `DUPLICATE`, or both edits applied. Every older applier test passed unchanged except the two schema-gate pins, which listed 6 as unknown.
- **2026-09-24, S11 — found while planning: a refused edit could still change the XML.** `_runs_covering` split the start boundary before finding that the end boundary sat in a run it could not split (a tab), leaving a split run behind the refusal (the same text, different runs). `_require_splittable_boundaries` now refuses before anything moves, and a new test proves the document's XML is untouched.
- **2026-09-24, S11 — the `finding_id` / `EditEntry.key` audit.**

  | Consumer | Where | Result |
  |---|---|---|
  | `EditEntry.key` | `applier/models.py` | No production consumer (one test). Now `(module_id, occurrence_id)` for schemas 6/7, `(finding_id, file_name)` for 4/5; the reader's repeated-key rule is its only use, and an AST test fails if any other applier code reads it. |
  | Conflict detection | `applier/conflicts.py` | Keyed on resolved places (paragraph span, insertion gap), never on ids: one finding's edits at two places are independent, two findings' edits at one place are not. |
  | Receipt and summary | `applier/models.py`, `applier/receipt.py` | Each outcome carries `finding_id`, now with `occurrence_id`, `location_basis`, `related_entries`; the summary shows the occurrence id. |
  | `--only` filter | `applier/policy.py`, `applier/cli.py` | A finding id selects every place that finding applies, now possibly several entries; an occurrence id selects one place. The receipt's `only_finding_ids` key is unchanged. |
  | Assist prompt | `applier/assist.py` | Carries `finding_id` as context for one entry at a time; unaffected. |
  | HTML finding anchors and chat links | `src/output/html_report_exporter.py` | **Defect, for S12:** anchors are `f-<finding_id>`, repeated when two modules of a program hold one id or two coordination findings are content twins, so `navigate_to_section` reaches only the first (`TestFindingAnchorsAreUnique`, two strict xfails). |
  | Drawing-impact links | `drawing_impact/`, both exporters | Program findings are qualified `module::finding_id`; unaffected by occurrences. |
  | Cross-check prompt, trace correlation | `cross_checker.py`, `tracing/` | Identify findings, not places; unaffected. |
  | Occurrence ids | `pipeline.compute_occurrence_id` | Built from the stamped finding id, never minting one (S02's tripwire). |
- **2026-09-24, S11 — updated on purpose.** `test_applier_sidecar.py`: the supported-set pin ({4, 5} → {4, 5, 6, 7}) and the unknown-version examples (6 replaced by 8, and the string "6" added). `test_contract_pins.py`: two docstrings that called S11's change future work. Nothing else moved; no golden changed, and one was added.

---

## Release-note lines (collected for S19)

Each session adds one plain line per user-visible change. S19 moves them into README.md.

- (S02) Findings that differ only in wording no longer merge because both mention a file name. "…requires copper pipe in 210500.docx" and "…requires PVC pipe in 210500.docx" are now two findings, while the same issue reported in several files still groups. Some finding IDs change because the old key was wrong; existing reports and sidecars are untouched.
- (S02) Edit applier: when two different supplied files share a file name, neither is edited (`FILE_AMBIGUOUS`), in either input order. An edited copy that would overwrite any supplied file, such as last run's `*.applied.docx` left in the folder, is refused (`DESTINATION_CONFLICT`). Both are decided before anything is written, the other files are still processed, and the run exits 3.
- (S03) Structure alerts: a clean spec no longer gets an "Empty section" alert for every PART heading, and lines such as "2 coats of primer shall be applied." are no longer read as headings or duplicate headings. A PART with no content anywhere is one alert, not one per empty article. An article with nothing before END OF SECTION is now flagged.
- (S03) Stale code-year alerts are no longer silenced by unrelated nearby words ("prior to fabrication", "historical society", "may not deviate from"). Citations described as old or rejected ("previously", "superseded", "shall not follow", "instead of") stay quiet, and each citation in a sentence is judged on its own. ASCE 7 written as ASCE/SEI 7-16, ASCE Standard 7-16, with an en or em dash, or as 7-2016 is recognized. For the California program, "2019 California Building Standards Code", "2022 Edition of the CBC", "CBC (2022 edition)", and "Title 24, 2022" are recognized too.
- (S03) A comma-separated list of code citations ("2019 CBC, 2019 CMC") is no longer reported with an extra, phantom citation.
- (S03) Placeholders: a bare "TBD" is now flagged, once. "[EDITION …]" and "[SELECTED …]" are no longer mistaken for EDIT and SELECT placeholders, and part numbers such as TBD-200 stay clean.
- (S03) File naming: compact (210500) and SECTION-prefixed names are recognized, names without a section number no longer hide a mixture, and when no style is used by most files the report says the styles are mixed instead of picking one.
- (S04) Ask AI chat: an answer that fails partway (an API error, a dropped connection, a cut-off or malformed response, a refusal, the length or tool limit, Stop, New chat, or a model change) is now shown as interrupted and left out of the conversation, and the question goes back into the message box. Before, some failures were silently kept as if the answer were complete, and a failure during a report-tool call could make every later message fail. An answer cut off at the length limit can no longer be continued by asking "continue"; ask a narrower question or choose a lower effort.
- (S04) Ask AI chat: the API key is now kept only in the page's memory, not in the browser tab's session storage. Reloading or closing the report forgets it, and a key an older report left in session storage is deleted when a report is opened. A browser that blocks or restricts storage no longer breaks the chat.
- (S04) Ask AI chat: web-search citations stay attached to the text they support, shown as numbered sources, and are kept when the conversation continues. A long web search that pauses while filtering its results now continues instead of failing.
- (S05) Verification: a claim the verifier could not settle ("Insufficient evidence") is no longer saved in the claim cache, so the next run checks it again instead of replaying the old answer for up to 60 days. Within one run it is still shared among identical findings. When the cache file loads, entries an earlier version saved this way are ignored, along with any row holding an invalid date or value, and the run log says how many; valid entries still load.
- (S05) Verification: a garbled or missing verifier reply (no verdict, an unknown verdict value, a malformed tool call, text with no valid verdict) and a reply that ran no web search now show as "Verification failed" instead of "Insufficient evidence", the same way in batch and real-time runs, and the tokens they used are in the cost estimate. A refusal, the output limit, and the context window are each named in the explanation. In real-time runs these replies are no longer escalated to Opus. A batch run whose polling stopped before a finding's wave finished marks that finding as failed, not as insufficient evidence.
- (S05) Verification: an empty or whitespace-only citation never counts as a source, so a "Disputed" backed only by blank sources shows as "Insufficient evidence".
- (S06) Cross-check and compliance are now sized with Anthropic's token-count estimate for the model that runs them (a padded local count when the estimate is unavailable), so a large package the old local count under-measured is split instead of being sent too large. A CSI division too large on its own is split into parts, and a specification that cannot fit is named as "not analyzed" in the log and the report, never cut short and sent. The pass's summary says when coordination was checked only within chunks or parts.
- (S06) Review size check: each spec is judged against the review model's own context window and output limit, on Anthropic's count estimate (or the padded local count when that is unavailable), and the error lists every spec that is too large, with its size, where the number came from, and the ceiling. A spec the API says fits is no longer refused because the padded local guess said otherwise.
- (S06) The fallback padding for Opus 5 and Opus 4.8 rose from 1.10× to 1.45× (they use the same tokenizer as Sonnet 5, which produces about 30% more tokens), and an unrecognized model pads by 1.50×. Large batch reviews get the 300k output allowance from that count, so a big spec is no longer left on the 128k limit because a raw local count ran low; real-time mode refuses such a spec by the same count and suggests batch mode.
- (S06) Anthropic's token counts are labelled as estimates ("API estimate") in the log, the token gauge, and the drawing cost dialog; they were called exact. A missing or malformed count is treated as unavailable, never as zero.
- (S07) Local-code compliance now says when its coverage is incomplete. Every controlling requirement gets a row in the Requirements Coverage table, and one the compliance model never classified shows as NOT ASSESSED instead of disappearing. A requirement is reported missing only when every part of the package was assessed: if a chunk failed or was too large to send, a chunk skipped the requirement, or a spec's own review failed, it shows as NOT FULLY ASSESSED. A red notice heads the Run Diagnostics banner and the coverage table, and a pass that assessed nothing no longer reads "no missing or contradicted requirements found".
- (S07) A compliance addition whose requirement was not established as missing from the whole package is now shown as report-only, with the reason and the proposed text, instead of being emitted as an edit; this also covers additions based only on unverified research or a process advisory. Before, such an addition could reach the edit sidecar, or an "unclear" result from another chunk silently removed it from the report.
- (S07) A requirements profile with no grounded specification requirements is reported as "no applicable requirements", not as a skipped compliance pass. Unverified research items no longer appear as rows in the coverage table (a model-returned row for one could read as MISSING).
- (S07) The edit sidecar and `.profile.json` carry a `requirements_coverage_completeness` record (per module in a program sidecar), and the compliance diagnostics event carries it too; the sidecar schema version is unchanged. Coverage rows gain `origin`, `assessment`, `reason`, and `also_reported`.
- (S08) A review repair batch (the automatic re-run of specs whose first review failed) that is still running when the results come in no longer loses its saved state. The report is marked provisional: a red row and notice name the repair batch, the specs waiting for it, and the stages deferred for it. Verification, cross-spec coordination, local-code compliance, and drawing-impact analysis wait, so they run once, on the final review, and collecting the run again picks the repair up instead of paying for a new one. In a Hyperscale program, one module's pending repair holds every module's later stages. A repair batch that can't be reached is treated the same way, and a saved repair is collected even when the source files have moved.
- (S08) Saved batch state follows one rule in the app and in `scripts/recover_batch.py`: it is kept while a repair is running or can't be reached, while a module couldn't be collected, or when every spec failed review, and otherwise it is cleared, but only if it belongs to the run that just finished. Recovering a batch by id in the app no longer deletes another run's saved state, and the app no longer clears a run in which every spec failed. Answering No to the resume prompt discards only the batch it described.
- (S08) `scripts/recover_batch.py` exits 0 when the run is complete and 2 otherwise, whatever the batch's final status. A batch recovered by id whose repair is still running is saved for a later resume when no other run's state is on disk, and **Recover batch…** given the id of a batch the app saved resumes it from that saved state, module and repair batch included.
- (S08) The edit sidecar carries `provisional` and the run's `collection` outcome (`collection_by_module` and `deferred_program_stages` in a program sidecar), and the review diagnostics event carries the outcome too; the sidecar schema version is unchanged.
- (S09) The cost estimate counts every paid request once, including the ones whose results were thrown away: a review the repair batch re-ran keeps its truncated first attempt (often the largest line of the run), a verification that restarted or finished in real time keeps the paid attempts before it, a cross-check that re-asked keeps both answers, and the Haiku triage calls are counted at all. A real-time fallback inside a batch run is priced at standard rates, not the batch discount. A long run no longer drops its earliest spend from the estimate.
- (S09) A request that failed before its usage was read, or a batch still running or out of reach, is named as "unknown usage — not in the estimate" instead of being counted as free; batch items the API reports as errored, canceled, or expired are not billed and count as zero. Collecting the same batch twice into one report counts it once.
- (S09) The estimate is labelled as one ("an estimate from the recorded usage at list prices, not an invoice") in the diagnostics export, in a new Estimated Cost block in the Diagnostics window, and in `scripts/recover_batch.py`, and it is broken down by operation (review, review repair, verification, escalation, triage, cross-check, compliance, drawing impact, research, drawing digest). A resumed or recovered run shows the batch it re-read, billed before the collection started, as earlier batch spend, apart from the collection's own spend.
- (S10) Review now sees text inside Word content controls (a template's filled-in values, a drop-down's chosen entry, and an unfilled control's placeholder, which the placeholder check now flags), the stored results of fields (a cross-reference such as "Section 23 05 00"), smart tags, and insertions tracked inside hyperlinks, in document order and with tracked changes accepted at every depth. Field codes are never read. Word's table of contents is skipped because it repeats the headings, and a text box is no longer read twice.
- (S10) Equations, embedded documents, legacy drop-down form fields, and tables inside a table's content controls are still not read; each now adds a "verify visually" extraction warning with a count.
- (S11) Edit applier: every edit in a document is now planned before any is written. Two instructions that change overlapping text, or add different paragraphs at one place, are both held and reported as `EDIT_CONFLICT`, each naming the other, instead of whichever came first in the sidecar winning. An identical instruction listed twice is written once and the copy reported as `DUPLICATE` (before, the copy read as "not found"). Edits that used to block each other depending on their order — one's new text repeating another's target, or changing an addition's anchor — now both apply. `--strict` counts a conflict as unapplied, not a duplicate.
- (S11) Edit applier: it reads sidecar schemas 6 and 7, which Spec Critic will write in a coming release to keep every place a fix is needed (the same fix at two places in one file becomes two instructions, each with an `oc-…` occurrence id), alongside today's 4 and 5; any other schema is still refused. `--only` also accepts an occurrence id, to apply one place. Receipt outcomes name their occurrence and, for a conflict or a duplicate, the other instructions involved. Spec Critic's own sidecar is unchanged in this release.
- (S10) Edit applier: text inside a content control is never edited; the edit is reported for hand editing. An edit that reaches into a field result, hyperlink, smart tag, or another author's revision, or that would move a field, control, or tracked deletion, is refused with its own reason. Before, an edit across another author's deletion could scramble what Reject All restores, and an edit touching a cross-reference's result could be duplicated or lost when Word updated the field. When the target text also appears inside a control, text box, or note and no element id confirms which copy was meant, the edit is held as ambiguous instead of going to the editable copy.

---

## Starting point

**Re-measured by S01** (2026-09-23/24) at master `6b48503`, the merge of PR #374. `src/`,
`applier/`, `tests/`, `scripts/`, and `evals/` are identical to `f9da027`. Spec Critic 3.9.0,
Anthropic SDK 1.7.0. No failures existed on master.

- **Python 3.11.15** (CI's version), in a fresh venv built from `requirements-dev.txt`. `python -m pytest -m "not network"`: **3,979 passed, 14 skipped, 10 deselected** in about 20 seconds, identical to the reference below. The same 14 container skips: 11 tkinter, 1 tiktoken rank file, 1 PyInstaller, 1 Playwright.
- **Python 3.12.3 with Tk**, so the tkinter suites run as they do in CI: **4,156 passed, 3 skipped** (rank file, PyInstaller, Playwright).
- `python plans/check_plan_status.py`, its last run before S01 deleted it: **25 OPEN, 0 FIXED, 0 ERROR**.
- **After S01:** 3.11: 4,159 passed, 18 skipped, 48 xfailed. 3.12 with Tk: 4,337 passed, 3 skipped, 51 xfailed. The 4 extra skips on 3.11 are the GUI probes, which need tkinter.

Container notes for later sessions:

- The system interpreter can't run the suite. Its Debian `cryptography` package has no `_cffi_backend`, so `pypdf` fails to import and two modules error at collection. Build a venv from `requirements-dev.txt` (then `pip install -e . --no-deps`) and run pytest from it.
- `python3.11-tk` can't be installed (the deadsnakes PPA is blocked), but `apt-get install -y python3-tk` adds Tk for Python 3.12. A 3.12 venv from the same lock file runs every GUI suite.

Reference measurement from the plan revision (2026-09-23, master `f9da027`): 3,979 passed, 14 skipped,
10 deselected in about 30 seconds; `plans/check_plan_status.py` 25 OPEN, 0 FIXED, 0 ERROR.

---

## Session log

Newest first. One entry per session: date, chunk, PR, what changed, test result, and what's left.

### 2026-09-24 — S11: Keep every edit location, part 1 (WP-06B)
- **PR:** [#385](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/385)
- Started at master `9919244` (the merge of #384). Both baselines matched S10's final numbers exactly: 3.11 had 5,412 passed, 19 skipped, 10 xfailed; 3.12 with Tk had 5,614 passed, 3 skipped, 13 xfailed (12 network tests deselected on each). No failures existed on master, and no strict xfail named S11 (see "Decisions and deviations").
- **Reproduced first**, with a scratch script against master: the same edit at p4 and p8 of one file was one occurrence (A1); occurrence ids followed input order (`grp-0000::000::a.docx` became `grp-0001::…` when the list was reversed, A2); a file with no original exposed another file's element for its edit (A4); identical findings in two modules had one occurrence id (A5); the applier refused schemas 6 and 7 (B1, B2) and `is_program` was false for 7 (B3); of two overlapping edits the first listed won (C1); an identical edit listed twice read as "not found" (C2); two additions at one anchor landed in list order (C3); and compatible edits blocked each other depending on order (C4, C5). The control (a duplicate emission at p4 is one occurrence) held. All twelve pass on the branch.
- **Occurrence model** (`src/orchestration/pipeline.py`). `group_findings` keeps one `FindingOccurrence` per file and target, validated against the reviewed text through `element_index_from_specs` or else claimed; duplicate emissions collapse into `members`; members with no usable element are one uncertain occurrence; a missing original is explicit and borrows nothing (`executable_proposal`); `compute_occurrence_id` is content-derived and module-qualified, and `group_id` is the finding id; `edit_occurrences` is a run's executable view with content twins collapsed. The schema 4/5 writer keeps its per-file expansion verbatim (`edit_sidecar._legacy_file_occurrences`).
- **Applier.** The reader takes schemas 4, 5, 6, and 7 (`applier/sidecar.py` defines the 6/7 contract; `EditEntry` gains `occurrence_id`, `location_basis`, `key`, `label`, `sort_key`). `DocumentEditor.plan` / `apply_planned` plan every edit against the unmutated document and write from the planned positions; the new `applier/conflicts.py` holds conflicting instructions (`EDIT_CONFLICT`) and writes identical ones once (`DUPLICATE`). The receipt, summary, `--strict`, and `--only` (occurrence ids too) follow.
- Tests: `tests/test_edit_occurrences.py` (57), `tests/test_applier_occurrence_schemas.py` (59), and `tests/test_applier_conflicts.py` (43) are new, with the golden `tests/goldens/legacy_sidecar_payloads.json` captured on master. `tests/test_plan_open_defects.py` gained `TestFindingAnchorsAreUnique` (two strict xfails for S12, two controls). Updated on purpose: `test_applier_sidecar.py`'s two schema-gate pins, and the `test_contract_pins.py` and `test_applier_isolation.py` bookkeeping (see "Decisions and deviations"). No existing golden moved.
- Mutation-checked: 70 breakages — 25 in the occurrence model, 3 in the legacy writer, 14 in the reader, 10 in the settlement, 4 in the writer's planning, 5 in the run, 8 in reporting, filters, and exit status, and one key-based decision against the audit's AST test — and every one turned a test red. Six survived the first pass (an original chosen by input order, an unknown basis accepted, additions compared by text alone, the action left out of an edit's identity, application re-finding its text instead of using the planned span, and the summary without occurrence ids); each exposed a test that could not tell, and each got a test that does.
- Docs: CLAUDE.md (the source layout; "FindingGroup vs FindingOccurrence" rewritten; the sidecar paragraph's known limit and reserved schemas; the applier's planning, conflicts, four schemas, outcome list, and test count), README (the applier bullets), `applier/README.md` (new "Which sidecars it reads" and "Instructions that disagree" sections; options; accounting), and handbook ch. 0, 2, 7, and 15.
- Tests (final): 3.11 had 5,574 passed, 19 skipped, 12 xfailed. 3.12 with Tk had 5,776 passed, 3 skipped, 15 xfailed. Both deselect 12 network tests. `pip check` was clean on both, and the JavaScript-required HTML suites passed with `SPEC_CRITIC_REQUIRE_HTML_TEST_TOOLS=1` (191, with the chat-behavior suite).
- Not done, by design (S12's): the writer moving to schemas 6 and 7, both exporters showing occurrence locations, the end-to-end report → sidecar → applier → receipt test, the handbook's sidecar row, and the HTML anchor defect found by the audit.
- **Next:** S12.

### 2026-09-24 — S10: Word content controls, fields, and smart tags (WP-02)
- **PR:** [#384](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/384)
- Started at master `92ef4d2` (the merge of #383). Both baselines matched S09's final numbers exactly: 3.11 had 5,253 passed, 19 skipped, 19 xfailed; 3.12 with Tk had 5,455 passed, 3 skipped, 22 xfailed (12 network tests deselected on each). No failures existed on master. Nine strict xfails named S10.
- **Reproduced first**, with a scratch script against master: 15 of 17 probes showed a defect — a block control's paragraph and table, inline and drop-down control text, an unresolved drop-down placeholder missing from the placeholder check, a REF field's result, smart-tag text, an insertion inside a hyperlink, controls with revisions in a paragraph and in a cell, a block control in a cell, row- and cell-level controls, a block control in a header, an edit aimed at control text landing on a plain copy (APPLIED), and a nested field's result inside an instruction read as prose ("Use SECRETCODEVALUEcopper pipe."). The two that held were legacy ids and a field's own instruction. All 17 hold on the branch.
- **Extraction.** One structural walk (`_paragraph_segments`, `_FieldState`) for every paragraph on every surface; block controls and custom XML blocks in the body, cells, rows, headers and footers, text boxes, and notes; the `cc` id namespace; the table-of-contents skip; text boxes read once; and `_Unsupported`, which counts the four unread kinds for the extraction warnings.
- **Applier.** `ElementKind.CONTENT_CONTROL` and `WRITABLE_KINDS`; the `cc` id grammar in the locator; an unwritable copy makes an id-less match ambiguous; the writer matches over the extractor's segments, refuses wrapped text with each wrapper's own reason, and refuses spans that would move structure; the assist tier can't choose an unwritable copy.
- Tests: `tests/test_extraction_content_controls.py` (73) and `tests/test_applier_wrapped_content.py` (36) are new: every WP-02 acceptance case, the field-state cases, controls in tables and in each story, the text-box alternates and grouped shapes, the warnings, each wrapper's refusal, spans across structure, identical text inside and outside a control (writer, locator, and assist), additions beside wrapped text, and runs end to end through `apply_sidecar` (dry run too). Extended: `tests/test_contract_pins.py` (104 → 120: the id grammar, and every `cc` id resolved against an independent reading of the XML), `tests/test_spec_docx_fixtures.py` (58 → 67), and `tests/test_applier_locator.py` (39 → 55). One locator test was replaced on purpose (see "Decisions and deviations"). No golden moved.
- Mutation-checked: 58 breakages — 38 in the extractor, 8 in the locator, models, and assist, 12 in the writer — and every one turned a test red. Two survived the first run (a row-level control between two ordinary rows renumbering the rows after it, and a cell-level control inside a wrapped row going unread) and got tests. The review fix for block-level equations added nine more, all caught.
- Docs: CLAUDE.md (a new "Word wrappers" part of "DOCX supplemental content extraction" with the supported and unsupported table, the text-box and gap notes, the locator's unwritable-copy rule, and the writer's "readable is not writable" rule), README (pipeline step 1, the applier section, testing), `applier/README.md`, and handbook ch. 4 (currency note and id table) and ch. 15 (test map).
- Tests (final): 3.11 had 5,412 passed, 19 skipped, 10 xfailed. 3.12 with Tk had 5,614 passed, 3 skipped, 13 xfailed. Both deselect 12 network tests. `pip check` was clean on both. The HTML report was not touched, so the JavaScript-required suites were not re-run.
- Not done, by design: reading tables and text boxes in headers and footers, tables in text boxes and notes, and SmartArt; recognizing a bare-field table of contents; looking up a legacy drop-down's chosen entry by its position; and field state across paragraphs (see "Decisions and deviations").
- **Next:** S11.

### 2026-09-24 — S09: Attempt accounting (WP-15)
- **PR:** [#383](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/383)
- Started at master `86edeea` (the merge of #382). Both baselines matched S08's final numbers exactly: 3.11 had 5,193 passed, 18 skipped, 19 xfailed; 3.12 with Tk had 5,393 passed, 3 skipped, 22 xfailed (12 network tests deselected on each). No failures existed on master, and no S09 strict xfails existed (see "Decisions and deviations").
- **Reproduced first**, with a scratch script against master (the fake batch service, the verification drivers, scripted streaming clients): a repair replaced the truncated primary's usage in the review total (A1, 128,800 output tokens billed, 800 recorded) and its cost (A2); collecting one batch twice doubled it (B1); a resumed run had no earlier-versus-own split (C1) and no estimate label (C2); a pending repair's usage was missing rather than unknown (D1); a real-time verification retry dropped the paid turn it abandoned (E1), a batch fresh retry dropped the paused waves (E2), the real-time fallback dropped the paid batch wave (E3) and was priced at the batch discount (E4); a cross-check parse retry overwrote the first response's usage (F1); a real-time review call that raised got no row of its own (G1) and the terminal one recorded zeros as if measured (G2). Found while reproducing: the event cap evicted billed spend from the estimate (H1: $2.50 of review spend read $0.00 after 5,000 progress events). A fifteenth probe, a per-call list past the per-event byte cap (H2), priced correctly on master. All fifteen pass on the branch.
- **Contract.** New `src/core/attempt_usage.py`: `AttemptUsage`, `known_attempt` / `unknown_attempt`, `known_totals`, `attempts_from`, the operation / role / transport / scope vocabularies, the spend categories, and `UsageSink`. `ReviewResult` gains `call_usage` and `message_id`; `VerificationResult` gains `transport`; `RepairOutcome` gains `attempts`; `BatchSubmission` gains `resumed`.
- **Producers.** Review collection (primary records before the repair merge, repair records at every exit, unknown for unread batches), the real-time runner (one record and one row per call, raised calls included), the verifier (abandoned conversations, retries across waves, the fallback, the detach safety net, unread escalations, worker crashes), cross-check (summed usage across its parse retry), the program merge, and triage (`usage_sink` from `verify_findings_for_run` down to `triage._classify_batch`, and `diagnostics.triage_usage_sink` in both drivers, both rounds). Research, drawing digest, cross-check, compliance, and drawing impact name their `operation`.
- **Diagnostics.** A billing ledger taken before the event caps and never evicted; one billing input per event; each attempt priced once on its own model and transport; duplicates counted once, a read copy over an unread one; unknown usage counted and named, never priced; `cost_summary` gains `estimate_note`, `by_scope`, `by_category`, `unknown_usage_attempts`, `duplicate_attempts_ignored`, `legacy_records`. `cost_summary_lines` is the one wording, used by `to_text()`, a new Estimated Cost block in the GUI Diagnostics window, and `scripts/recover_batch.py`.
- Tests: `tests/test_attempt_accounting.py` (60) and `tests/test_attempt_accounting_gui.py` (2) are new: the record's contract; primary plus repair; a repair that fails again; a refusal with usage; an errored item as a known zero; pending and unreachable repairs; plan §28 scenario C's accounting clause (a pending repair collected later, after a restart and into one report); duplicates both ways; scope for a resumed primary, a new repair, and a re-attached one; the estimate label; every price line once on either transport; flat fields never added; per-attempt transport and model; real-time totals unchanged, raised calls, and a retry after one; the cross-check retry; the real-time retry, batch fresh retry, fallback, detach, unread escalation, escalation category, and worker crash; cache-hit and shared followers; triage (success, exception, a failing sink, the diagnostics sink, the pre-pass, and an AST pin that all four production verification calls pass a sink); the ledger under the event and byte caps; legacy records; the program merge; the GUI collect's pricing and the Diagnostics window's lines. Updated on purpose: `test_diagnostics_cost_pricing.py`, `test_verification_token_telemetry.py`, `test_batch_escalation.py`, `test_diagnostics_concurrency.py`, and `test_recover_in_progress.py` (see "Decisions and deviations"). No golden moved.
- Mutation-checked: 42 breakages, and every one turned a test red — in review collection (the repair records, the pre-merge primaries, the original defect restored, primaries read as unread, scope for a resumed primary and a re-attached repair, the resumed flag, the carrier totals, the real-time gather, the repair role, the pending repair's records, errored items, the crash record, a driver's triage sink), diagnostics (dedup, read-over-unread, the attempt input, per-transport pricing, the unknown count, the ledger before the byte cap, the scope lines, the label, the triage sink, legacy flagging), the verifier (the abandoned conversation, the raised call, prior waves, the retry context, the fallback's waves and role, the in-flight wave, the unread escalation, the pre-pass sink), triage (success, exception, a failing sink), the real-time runner (raised calls, the spec's records), cross-check, the program merge (records and the TTL split), the Diagnostics window, and the recovery CLI. One pattern failed to apply at first (a mistyped target string) and was fixed and caught.
- Docs: CLAUDE.md (a new "Attempt accounting (plan WP-15)" section; the source layout; the failure-usage, review-carrier, histogram, propagation, recovery-CLI, real-time diagnostics, and escalation-pricing paragraphs; the §9 fixture note; §10's baseline and recovered-runs notes and the rollback note), README (a new "What the Cost Estimate Counts" section and the recovery paragraph), and handbook ch. 7, 10, 13, 14, and 21.
- Tests (final): 3.11 had 5,253 passed, 19 skipped, 19 xfailed (the 18 container skips plus the new GUI module). 3.12 with Tk had 5,455 passed, 3 skipped, 22 xfailed. Both deselect 12 network tests. `pip check` was clean on both. The HTML report was not touched, so the JavaScript-required suites were not re-run.
- Not done, by design: per-call identity for the passes that record one aggregate (research, drawing digest, cross-check, compliance, drawing impact — see "Decisions and deviations"), and an invoice-level reconciliation, which no local record can provide.
- **Next:** S10.

### 2026-09-24 — S08: Paid repair recovery (WP-14)
- **PR:** [#382](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/382)
- Started at master `277640c` (the merge of #381). Both baselines matched S07's final numbers exactly: 3.11 had 5,093 passed, 18 skipped, 19 xfailed; 3.12 with Tk had 5,271 passed, 3 skipped, 22 xfailed. No failures existed on master, and no S08 strict xfails existed (see "Decisions and deviations").
- **Reproduced first**, with a scratch script against master (a fake batch service counting paid calls, fake GUI apps, and the real CLI): the GUI cleared the saved record while the repair batch was still running (A1), ran paid verification before the repair settled (A2), and carried no structured outcome (A3); a GUI collection of one batch deleted another run's saved record (B); the GUI cleared an all-failed run that the CLI kept (C); the CLI cleared the record and exited 0 while the repair was still running (D); a routed program cleared its manifest while one child's repair was pending (E1) and ran its children's paid verification before a sibling's repair settled (E2); two children stamping their repairs at once lost one stamp (F); and a saved repair was ignored when the extracted specs were unavailable (G). All ten probes pass on the branch.
- **Contract.** New `src/orchestration/collection_outcome.py` (stdlib-only): `RepairOutcome` (six states, two outstanding), `CollectionOutcome` (`reportable`, `remote_settled`, `provisional`, `all_failed`, `awaiting_repair_specs`, `to_dict`), the stage names and labels, the provisional wording, `decide_saved_state_cleanup` / `CleanupDecision`, and the identity helpers. `CollectedBatchState` and `PipelineResult` gain `collection_outcome`; `ProgramPipelineResult` gains `collection_outcomes`, `provisional`, and `deferred_program_stages`.
- **Collection.** The repair pass returns `(results, RepairOutcome)`; re-attaching runs before the source-file check and reads names from the request map; a failed read of an existing repair is `unreachable`. `defer_collection_stages` / `provisional_batch_result` hold verification, cross-check, compliance, and drawing impact back; the headless driver splits into `collect_review_state_headless` plus the rest; `collect_program_results` runs every module's review phase, then the dependent stages only when nothing is outstanding.
- **Saved state.** `batch_resume` gains `_STATE_LOCK`, `record_repair_batch`, `record_identity` / `pending_identity`, `clear_saved_state_for`, `apply_saved_state_cleanup`, `discard_saved_state`, and `adopt_outstanding_run`. The GUI's single-module and program branches and `scripts/recover_batch.py` apply the same rule; the CLI exits 0 exactly when the run is complete. The resume prompt names a recorded repair and discards only what it showed; **Recover batch…** resumes a saved id from its record.
- **Outputs.** A red banner row and notice in both exporters (shared helpers), a failed-review hint that leaves out pending specs, the program roll-up, the sidecar's additive keys, the review diagnostics event, the trace summary, the GUI's amber finish and log line, the review error for an awaiting spec, and the About dialog's step 7.
- Tests: `tests/test_repair_recovery.py` (100, after review) and `tests/test_repair_recovery_gui.py` (22, after review) are new, both driven through the new `tests/fixtures/batch_service.py`: the outcome contract; the cleanup decision for every case; identity-checked clearing (another run's record, a record naming an unsettled repair, a replaced repair, unreadable and newer-schema records, a legacy record, program manifests, explicit discard); concurrent stamping under the lock; re-attaching without source files; headless deferral with paid-call counts; plan §28 scenario C through the recovery CLI (a pending repair survives a restart and is collected once, each stage runs once, and only then is the state cleared; its "accounts for both attempts" clause is S09's); a temporary retrieval failure; `--keep-state`; all-failed; bare-id adoption; the parity table through the CLI, the headless driver, and the GUI; program children; the report, HTML, and sidecar surfaces; and the GUI's collect, resume prompt, and Recover dialog. Updated on purpose: `test_review_repair_hardening.py`, `test_program_pipeline.py`, `test_recover_in_progress.py`, and `test_deterministic_checks.py` (see "Decisions and deviations"). No golden moved.
- Mutation-checked: 61 breakages, and every one turned a test red — in the outcome contract (the outstanding set, `reportable`, `all_failed`, the settled ids), the cleanup rule (each keep condition, keep-on-request, real time), the identity check (a foreign primary, an unsettled or replaced repair, a program record's repair ids, an unidentifiable record, explicit discard), the lock around the stamp, re-attaching and its dispositions, the repair state for each failure, deferral in each driver and in the program phases, adoption, the Recover dialog and resume prompt, the completion handler, the CLI (exit code, `--keep-state`, adoption), and each report, sidecar, and diagnostics surface. Two survived at first: a manifest child naming an unsettled repair and a provisional program's `partial` status had no test of their own; both got one.
- Review: the Codex bot left one P1 finding, and it was fixed (see "Decisions and deviations"). A saved run whose repair stamp failed to save kept an unstamped record in the CLI, so the next run paid for a second repair; routed programs had the same gap in the GUI and the CLI. Three new tests failed before the fix (the CLI's saved run and the GUI's program run reproduced the second repair, and the GUI's single run lacked the new log line), and seven more pin the shared step: kept single and program runs, a record already stamped, another run's record or none (never stamped, never created, nothing logged), a complete run, a failed re-stamp, and a program resumed once end to end. A further test covers a record deleted mid-run, and the bare-id test now checks the record's input folder. 11 mutations of the fix were each caught; three survived at first (a foreign-record check that only a silent-skip assertion distinguishes, and the CLI's two input stashes) and each got a test. The original 61 were re-run against the new code: 58 are caught as written or retargeted (three named code this fix changed), and the other three named code this fix replaced and are covered by three of the 11 new mutations, so 69 ran in all.
- Docs: CLAUDE.md (a new "Paid repair recovery (plan WP-14)" section, the recovery paragraph, the source layout, the high-level flow, the real-time and routed-program paragraphs, the banner and failed-review sections, and the §9 fixture note), README (the recovery paragraph and the sidecar note), handbook ch. 3, 6, 7, 11, 13, 21, and 22, and the GUI About dialog.
- Tests (after the review fix): 3.11 had 5,193 passed, 18 skipped, 19 xfailed (the same 18 container skips). 3.12 with Tk had 5,393 passed, 3 skipped, 22 xfailed. Both deselect 12 network tests. `pip check` was clean, and the JavaScript-required HTML suites passed with `SPEC_CRITIC_REQUIRE_HTML_TEST_TOOLS=1` (191, with the chat-behavior suite).
- Not done, by design: accounting for every paid attempt, including a repair that replaces its primary and a later collection's re-read (S09, WP-15); per-module stage checkpoints, so a module error in the second phase still repeats its siblings' dependent stages on resume; and the applier holding a provisional sidecar's edits (see "Decisions and deviations").
- **Next:** S09.

### 2026-09-24 — S07: Compliance coverage completeness (WP-09)
- **PR:** [#381](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/381)
- Started at master `456eec2` (the merge of #380). Both baselines matched S06's final numbers exactly: 3.11 had 5,037 passed, 18 skipped, 19 xfailed; 3.12 with Tk had 5,215 passed, 3 skipped, 22 xfailed. No failures existed on master, and no S07 strict xfails existed (see "Decisions and deviations").
- **Reproduced first**, with a scratch script against master (a scripted streaming client and count API): an empty coverage list for two controlling requirements came back `completed` with no trace of either, and the Word report said "Compliance evaluation completed — no missing or contradicted requirements found" with an unhighlighted banner; a requirement the model left out was simply absent, while a returned `unclear` was a row; a row for an `[UNVERIFIED]` item reached the matrix; a failed chunk left another chunk's `missing` standing and its ADD executable; an `unclear` from one chunk silently dropped another chunk's ADD; a profile with no grounded requirements read as a red `skipped`; a single-pass ADD for a requirement with no coverage row stayed executable; and a spec excluded because its review failed did not stop a single pass from calling a requirement missing. All eight probes pass on the branch, and the report shows the notice, the banner suffix, NOT ASSESSED cells, and an honest subtitle.
- **Contract.** New `src/compliance/completeness.py` (stdlib-only): `AssessmentUnit`, `reconcile` (one row per expected requirement with `origin` / `assessment` / `reason` / `also_reported`; `missing` only when every unit returned it and nothing went unassessed), `CoverageCompleteness` (expected / returned / omitted / partially-assessed ids, unassessed specs, ignored rows, held additions, `state`, `complete`, `to_dict`), `combine` for programs, and `gap_phrases`. `ReviewResult.coverage_completeness` is the one new field.
- **Checker.** `expected_coverage_ids`; `_normalize_coverage` against the expected set (ignored rows counted, invented statuses noted); `_settle_additions` (keep an established `missing`, keep the chunked D-7 drop and dedup, hold everything else with the reason and proposed text); `coverage_finalizer` for the chunked merge; `ensure_coverage_completeness`; zero controlling requirements is `completed` / `no_applicable_items` with no call. The prompt, the schema description, and the examples now name the controlling set only.
- **Engine and stage.** `run_chunked_pass`'s `coverage_merge` / `finding_filter` became one `finalize` hook over every planned chunk, called after the error is set. `run_compliance_for_batch` passes failed-review specs as `excluded_specs`, stamps every skip, and ensures a record on whatever it stores.
- **Outputs.** Shared helpers in `report_exporter` drive both exporters: the banner row and hints (red notice), the coverage-table notice and NOT ASSESSED / NOT FULLY ASSESSED cells, the evidence cell (reasons and `also_reported`), the findings subtitle, and a held-addition note. The sidecar, `.profile.json`, the HTML payload, the compliance diagnostics event (`compliance_pass_extra`, both drivers), the run log, the trace span, and the program merge and banner roll-up all carry it.
- Tests: `tests/test_compliance_completeness.py` (55, after review) is new: the expected set and prompt surfaces, normalization (omitted vs unclear, ignored, duplicate, reordered, invented status), zero expected, chunk semantics (failed, not analyzed, omitting, unclear, unanimous, disproven, all failed, excluded specs, the closed status set), held additions, every report and JSON surface, diagnostics and the run log, the pipeline stage's paths, plan §28 scenario B through the headless driver, and program reports. Updated on purpose: `test_compliance_pass.py` (the merge tests rewritten against `reconcile` / `_settle_additions`, the zero-requirement test, the normalization and partial-failure expectations, plus one new hold test), `test_chunked_pass_engine.py` (the hook tests), and `test_diagnostic_banner.py` (the roll-up dict gained the coverage keys). Goldens: `dc_compliance_system_prompt.txt` (the `<coverage_rules>` paragraph) and `dc_compliance_user_message.txt` (the unverified header and the `<final_task>` coverage line), reviewed line by line; no California golden moved.
- Mutation-checked: 57 breakages, and every one turned a test red: 16 in `completeness.py` (no downgrade, either partial source ignored, no synthetic row, a synthetic `missing`, locations discarded, failed units' specs or rows trusted, rows fabricated when nothing completed, the state ignoring unassessed specs, empty or unknown expected sets reading complete, `combine` skipping a missing record or losing module labels, each gap phrase), 17 in the checker (unverified rows accepted, ignored rows or invented statuses not recorded, holds dropping, the single-pass drop, advisory and no-id holds, dedup, `unclear` as disproven, the proposed text, zero expected as `skipped`, failed chunks invisible to the finalizer, excluded specs lost on either path, three prompt and schema reversions), 2 in the engine, 4 in the pipeline stage and log, 1 in the program merge, and 17 in the exporters, sidecar, profile export, and diagnostics. Two survived at first: the finalizer's own completed-only guard was redundant (`reconcile` already reads only completed units, so the guard was removed and the mutation retargeted at `reconcile`), and the NOT ASSESSED cell was asserted with `in text`, which the notice's own wording satisfied; the tests now read the table cells.
- Review: the Codex bot left one P2 finding, and it was fixed. The "content not available" skip (a recovery with a saved profile but no re-extracted specs) left `unassessed_specs` empty although `files_reviewed` named the specs; the profile-unavailable skip had the same gap (see "Decisions and deviations"). Both skips now name the reviewed files. Two new tests failed before the fix; 2 more mutations (each skip losing its names) were each caught, and the 57 earlier ones still are (one had its pattern retargeted at the reshaped call).
- Docs: CLAUDE.md (a new "Compliance coverage completeness (plan WP-09)" section, the source layout's `compliance/` entry, the compliance-pass and report/sidecar bullets, the examples paragraph that described the old merge, and the HTML parity list), README (the compliance-pass bullet), handbook ch. 19 (the absence rule, a new "Knowing what was not assessed" section, the sidecar paragraph, the pins) and ch. 15 (a test-map row), and the GUI About dialog's compliance paragraph.
- Tests (after the review fix): 3.11 had 5,093 passed, 18 skipped, 19 xfailed (the same 18 container skips). 3.12 with Tk had 5,271 passed, 3 skipped, 22 xfailed. Both deselect 12 network tests. `pip check` was clean, and the JavaScript-required HTML suites passed with `SPEC_CRITIC_REQUIRE_HTML_TEST_TOOLS=1` (191).
- Not done, by design: coordination across chunks or modules (S25, EX-06); the banner's "REPORT_ONLY demotions at parse time" label, which also counts post-parse demotions (a Known-wrong row for S18); and `src/research/`'s missing entry in CLAUDE.md's source layout (S18).
- **Next:** S08.

### 2026-09-24 — S06: Request budgets (WP-08)
- **PR:** [#380](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/380)
- Started at master `67fde0e` (the merge of #379). Both baselines matched S05's final numbers exactly: 3.11 had 4,954 passed, 18 skipped, 19 xfailed; 3.12 with Tk had 5,132 passed, 3 skipped, 22 xfailed. No failures existed on master, and no S06 strict xfails existed (see "Decisions and deviations").
- **Reproduced first**, with a scratch script against master (a word-count stand-in for the local tokenizer, a scripted client): a count response with no `input_tokens`, a zero, or `None` came back from `count_tokens_via_api` as a trusted `0`; the cross-check chose a single call on a raw local count of ~700k (under 822k) and sent it without asking the count API, which would have said 950k; four Division 23 specs over the limit were skipped whole ("did not produce more than one viable chunk"), never split; a ~160k-word spec on Opus 5 stayed on the 128k cap, because the extended-output decision read the raw local count; and Opus 5 / Opus 4.8 padded by 1.10×. The same script on the branch: `None` for all three counts, 1.45× padding, the oversized package measured and not sent, the Division 23 package planned into measured parts (here none fit, so each spec is named as not analyzed), and 300k for the large spec.
- **Contract.** New `src/core/request_budget.py`: `RequestBudget` (count, count source, model, window, output reserve, safety reserve, phase limit, input ceiling, fit, local count, padding factor, unavailability reason), `count_request_from_params` (the counting form, derived from the request's own params), `resolve_input_count` (cached API estimate → API → padded local → unavailable), the per-shape count cache, and `oversize_reason`. `tokenizer.count_input_tokens` / `validated_input_tokens` replace the zero-returning helper, which is now a thin wrapper; the padding factors follow the tokenizer.
- **Review.** `review_request_builder`: one sizing rule (`review_input_count` → `_allow_extended_output` → cap → `review_request_budget`); the builder reads cached estimates only, and the real-time gate reads the same count for the initial and repair shapes. `pipeline._review_preflight_budgets` replaces `_run_exact_token_preflight` and the separate local gate, and `_prepare_specs` raises one error naming every oversized spec.
- **Package passes.** `build_cross_check_request` / `build_compliance_request` build each request once; `request_budget_for` sizes it (one gated count call, no-retry client); `chunked_pass.plan_chunks` measures each CSI group's real chunk request and splits an oversized group by bisection; a spec that cannot fit becomes a skipped, named chunk that never reaches the runner; a scope note heads the combined summary; `run_cross_check` / `run_compliance_check` recheck their own request.
- **Wording.** "Exact" became "estimate" for API counts in code, logs, the GUI gauge, the digest dialog, and the docs. The Known-wrong row for `tokenizer.py` / `pipeline.py` is ticked.
- Tests: `tests/test_request_budget.py` (83, after review) is new, with `tests/fixtures/count_api.py` and a `tests/conftest.py` guard that blocks the live count endpoint and clears the count cache per test. Updated on purpose: `test_token_budgets.py` (factor pins; the API-estimate gate), `test_cross_check_chunking.py`, `test_chunked_pass_engine.py` (the engine's layering allow-list now includes `request_budget`), `test_compliance_pass.py`, and `test_collection_call_gate.py` (forcing a split now scripts count answers, and permit counts include count calls), `test_realtime_review.py`, `test_drawing_digest.py` (dialog wording), and `test_network_smoke.py` (two network-only count-shape smokes; deselected offline, so 12 deselected instead of 10).
- Mutation-checked: 47 breakages, and every one turned a test red. 8 in the tokenizer (zero, bool, and missing counts accepted; the permit taken before the count method is resolved, or not taken; Opus 5 and Opus 4.8 back at 1.10×; the unknown default narrowed), 13 in the budget (tools, `tool_choice` / `thinking`, or tool JSON not counted; no tool allowance; a digest that ignores the model; the raw count used unpadded; `use_api=False` still calling the API; the preflight switch ignored; the cache never consulted; the output reserve, phase limit, or safety reserve ignored; an unsized request counted as fitting), 8 in the planner (no subdivision, a prefix off by one, an unsplittable spec sent, `min_specs` ignored, not-analyzed chunks reaching the runner, findings labelled by part id, failed coverage not named, parts sharing one id), 5 in cross-check (the API estimate ignored, a different shape measured than sent, no recheck, the count not gated, the scope note dropped), 2 in compliance, 6 in the review builder and real-time gate, 4 in the pipeline preflight (the double gate kept, the API never asked, only the first oversized spec named, the fallback not reported), and 1 in the digest dialog. A few survived at first — among them `use_api=False` still calling the API and findings labelled by part id — and each got a test before the final run.
- Review: the Codex bot left one P2 finding, and it was fixed. With cross-check's two-spec minimum, taking the largest fitting run could strand a lone spec at a part boundary (any three of four specs fit → 3 + 1, the last not analyzed) although 2 + 2 covers every spec (reproduced first: the two new borrow cases failed before the fix, while the three new guard cases, which pin what must not change, passed; a sixth test pins that both new parts are measured). A short run now borrows from the part before it when both new parts measure as fitting (see "Decisions and deviations"). 7 more mutations (the borrow disabled, either new part not measured, the lender allowed below the minimum, lending from the front, the joined run out of order, borrowing only at a group's end) were each caught, and the 47 earlier ones still are.
- Docs: CLAUDE.md (source layout, the flow, a rewritten "Token preflight raises (not warns): request budgets" section, cross-check chunking, the real-time gate, the drawing digest's `exact`, the model padding notes, retry layering, compliance chunking, §6, and §9), README (pipeline steps 4 and 7, Processing Mode, and a new "Request Sizing: API Estimates, Padding, and Chunking" section), and handbook ch. 2, 3, 7, 8, 12, 15, 19, and 21.
- Tests (after the review fix): 3.11 had 5,037 passed, 18 skipped, 19 xfailed (the same 18 container skips as master). 3.12 with Tk had 5,215 passed, 3 skipped, 22 xfailed. Both deselect 12 network tests, the two new count-shape smokes among them. `pip check` was clean. The HTML report was not touched, so the JavaScript-required suites were not needed.
- Not done, by design: retry timing for count calls (S15, WP-11; a package-pass count is one attempt and falls back to the padded estimate), compliance completeness semantics (S07), and coordination across chunks (S25, EX-06).
- **Next:** S07.

### 2026-09-24 — S05: Verification failures and cache (WP-10)
- **PR:** [#379](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/379)
- Started at master `577f578` (the merge of #378). Both baselines matched S04's final numbers exactly: 3.11 had 4,656 passed, 18 skipped, 22 xfailed; 3.12 with Tk had 4,834 passed, 3 skipped, 25 xfailed. No failures existed on master.
- **Reproduced first**, with a scratch script that drove both transports on master: a grounded UNVERIFIED was cached and replayed on the next lookup; text with no JSON after a searched turn was a grounded, cacheable UNVERIFIED in real time but a failure on batch; a verdict call with no `verdict` field, or with `"PROBABLY"`, was a grounded, cacheable UNVERIFIED on both; a verdict call whose input was not an object was a clean UNVERIFIED in real time and a failure on batch; every real-time failure reported zero tokens; a DISPUTED backed only by a whitespace source rendered as DISPUTED; and one string timestamp in the cache file raised out of `load_from_disk`, so the run started with an empty cache.
- **Classification.** `verifier.classify_verification_turn` classifies every finished conversation on both transports, and `_stamp_verdict_result` / `_failure_result` build the result; only pausing, retrying, and batching stay per transport. `VerificationResult.outcome` (runtime-only) names the kind. A malformed verdict (input that is not an object; a missing, null, or unknown `verdict`; conflicting calls; text with no valid verdict), a turn with no search or fetch result, and every incomplete stop (refusal, `max_tokens`, the context window, anything else) is a failure: UNVERIFIED, ungrounded, never escalated, never cached, with the conversation's usage and evidence kept. The batch loop's own terminals (continuation cap, non-retryable error, unresolved, crashed fallback, the safety net) use the same builder, and "no API key" and the safety net are now failures.
- **Cache.** `verification_cache.cache_ineligibility_reason` is the one predicate at `put`, `get`, and `load_from_disk`. Only a grounded CONFIRMED / CORRECTED / DISPUTED with a substantive citation (and a quote, for CONFIRMED / CORRECTED) is reused, and a replay is never re-stored. The load checks each record on its own (timestamps, field types, counts), ignores the bad ones, and counts them in `stats()["rejected_on_load"]`; the pipeline's load log line reports the count. No schema bump.
- **Sharing.** `pipeline._shareable_verdict` shares an UNVERIFIED in-process only when the verifier stamped `outcome == "verdict"` and it is not failed, budget-exhausted, or local. A shared follower's diagnostics event is not an API call.
- **Sources.** `source_grounding.is_substantive_source` (a value `normalize_url` can reduce to something) is used by the grounding invariant, the cache predicate, and `classify_status`.
- Tests: `tests/test_verdict_classification_contract.py` (138) and `tests/test_verification_cache_eligibility.py` (138) are new, with `tests/fixtures/verification_drivers.py`, which drives the real `verify_finding` and the real batch wave loop from scripted responses. `test_verification_singleflight.py` gained 12 WP-10 tests. The source-string pins in `test_budget_exhaustion.py` and `test_verification_failed_status.py` became behavioral tests, with real SDK exceptions. `test_verification_stop_parity.py` was updated on purpose (named outcomes, kept usage). The 3 S05 strict xfails (2 markers) became regression tests.
- Mutation-checked: 41 breakages, and every one turned a test red. 21 in the verifier (the old unknown-verdict coercion, a missing verdict tolerated, a malformed call falling through to the text, conflicting calls not detected, text without a verdict read as UNVERIFIED, no evidence gate, each of the three incomplete stops not named, usage dropped by each transport, by a failed continuation, and at the batch continuation cap, fetch results ignored by the batch gate, a failure not flagged, dict text blocks ignored, blank sources counted by the invariant, a failed escalation replacing the first pass, the safety net and no API key not failures, budget exhaustion not stamped), 12 in the cache (UNVERIFIED reused, blank citations counted, replays re-stored, `get` or the load not applying the predicate, non-finite, future, string, and missing timestamps accepted, field types not checked, rejected rows not counted, blank list entries kept), 6 in the pipeline (sharing ignoring the outcome, excluding a grounded UNVERIFIED, or inheriting a failure, a budget shortfall, or a local result; the load log omitting ignored rows), 1 in `classify_status`, and 1 in diagnostics. One (sharing inheriting a failure) survived at first; parametrized sharing cases now catch it.
- Docs: CLAUDE.md (a new "Verification outcomes: one classification contract, one cache predicate" section; the grounding-invariant cache bullet; the DISPUTED quote paragraph; routed-program single-flight; the budget-exhaustion sentence; incomplete-stop parity; the exactly-once safety net; two trust-table rows; the §9 fixture bullet), README (design bullets, pipeline step 6, and a new "Verification Failures, Uncertainty, and the Claim Cache" section), handbook ch. 10 (the parser, a new "One classification contract" section, the gates, the cache refusal table, the schema version, the trust rows, the takeaways), and ch. 9, 11, and 15.
- Tests: 3.11 had 4,954 passed, 18 skipped, 19 xfailed. 3.12 with Tk had 5,132 passed, 3 skipped, 22 xfailed. `pip check` was clean, and the JavaScript-required HTML suites passed with `SPEC_CRITIC_REQUIRE_HTML_TEST_TOOLS=1`.
- Not done, by design: an attempt abandoned for a retry is still not added to the final result (a real-time retry after partial responses, a batch fresh retry after a paused conversation, the real-time fallback after paid batch waves). That is S09's attempt model; see "Decisions and deviations".
- **Next:** S06.

### 2026-09-24 — S04: Chat (WP-12)
- **PR:** [#378](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/378)
- Started at master `01027c3` (the merge of #377). Both baselines matched S03's final numbers exactly: 3.11 had 4,563 passed, 18 skipped, 23 xfailed; 3.12 with Tk had 4,741 passed, 3 skipped, 26 xfailed. No failures existed on master.
- **Reproduced first.** The new harness, run against master's script, showed the P1-5 case end to end: an `error` event after a partly streamed `tool_use` produced no notice, and the next request carried that `tool_use` with `input: {}` and no `tool_result` (the API rejects that, so every later message fails). The tool-round limit left the same orphan. Also on master: CRLF or CR endings split across reads lost the whole answer, EOF counted as success, citations were never attached to their blocks, throwing storage stopped the script at load, and the `pause_turn` continuation sent no `container`. 71 of the 88 new tests failed on master; the 17 that passed are happy paths, LF streams, and preference handling.
- **Stream and errors.** An SSE line parser (CRLF / LF / CR, a CR at a chunk end waits, multi-line `data:`, comments ignored, an unterminated final event never dispatched) with a fatal UTF-8 decoder flushed at the end. Each event is checked as it arrives; a response counts only at `message_stop` with a stop reason and every block closed. Only `JSON.parse` is guarded. Tool input is parsed strictly at `content_block_stop`.
- **Transaction.** Turns build up in `turn.messages` and commit only on `end_turn` / stop sequence (`commitTurn`); every other ending discards the turn (`finishTurn`, which closes a turn exactly once, aborts its request, and restores the controls). History trimming drops whole turns. The partial answer is marked "Interrupted", and the question returns to the message box. Stop, New chat, a model change, Forget key, and `pagehide` close the turn at once, and a closed turn's later reads are dropped. Every stop reason and both loop limits are handled visibly.
- **Citations and blocks.** `citations_delta` is appended to its own text block's `citations`, replayed verbatim, and shown as numbered sources; a citation without an https URL is replayed but never shown. Thinking signatures and web search results replay byte-for-byte. The `container` id reaches later requests within the turn.
- **Key.** Page memory only; `prefRemove("sc_api_key")` on load; Forget key and `pagehide` clear it; guarded preference storage (`prefStore` / `prefGet` / `prefSet` / `prefRemove`); stale model preferences are ignored.
- Tests: `tests/test_html_chat_behavior.py` (92) is new, with `tests/fixtures/chat_harness.{js,py}`. The S04 strict xfail became a regression test on the new harness, and the old probe was deleted. Three exporter string pins were updated.
- Mutation-checked: 40 breakages of the shipped script, and every one turned a test red: swallowed handler errors, `{}` for bad tool JSON, non-object input, EOF / open blocks / missing stop reason accepted, a CR ending a line early, unknown events rejected, events before `message_start`, error events ignored, a non-fatal decoder, failed turns committed, stale reads processed, a non-idempotent close, Stop waiting for the promise, no abort on early endings, model change not stopping the turn, the key written to storage, the legacy key re-imported, Forget key and `pagehide` keeping it, unguarded storage reads and access, stale model preferences, citations not attached / not marked / non-https shown, no container, both loop limits off by one, `max_tokens` / refusal / unknown stop / empty answers / unresolved web tools / unsigned thinking committed, no required-field check, no trimming, the question not restored, and the answer not marked. One (unguarded storage access) survived at first and exposed the harness's `vm` accessor problem (see "Decisions and deviations").
- Docs: CLAUDE.md ("HTML report + Ask AI": transaction model, stream contract, key policy; §9 harness bullet; §10 live-run row), README (Ask AI and Testing), handbook ch. 22 (a new section on the transaction, the key policy, and the pins) and ch. 15 (a test-map row), and the exporter's module docstring.
- Review: the Codex bot left one P1 finding, and it was fixed. A response that ended with `end_turn` or a stop sequence but still carried a report-tool call was committed without a `tool_result`, so every later request in that conversation would be rejected (reproduced first; 3 new tests failed before the fix, and a fourth pins the valid case, a web call answered after a report-tool round). The commit check that covered only web tool calls now covers every call in the turn: a report-tool call is answered only after a `tool_use` stop, so one left in a finished or paused response is malformed and discards the turn, instead of being run late or stripped from what the API sent. 5 more mutations (report calls not checked, only the last response checked, tool results not counted as answers, user messages skipped, a web call required to be answered in its own response) were each caught, and the web-tool mutation was retargeted at the new check. CLAUDE.md and handbook ch. 22 now state the check.
- Tests: 3.11 had 4,656 passed, 18 skipped, 22 xfailed. 3.12 with Tk had 4,834 passed, 3 skipped, 25 xfailed. `pip check` was clean, and the JavaScript-required HTML suites passed with `SPEC_CRITIC_REQUIRE_HTML_TEST_TOOLS=1`.
- Not done, by design: no live run against the real API (CLAUDE.md §10 still lists it), and no headless-browser smoke.
- **Next:** S05.

### 2026-09-24 — S03: Detectors (WP-04)
- **PR:** [#377](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/377)
- Started at master `0a2aef3` (the merge of #376). Both baselines matched S02's final numbers exactly: 3.11 had 4,256 passed, 18 skipped, 44 xfailed; 3.12 with Tk had 4,434 passed, 3 skipped, 47 xfailed. No failures existed on master.
- **WP-04A.** `heading_candidates` returns qualified `HeadingCandidate`s (number, title, level, position, `run_in`, provenance). A heading needs `PART n` or a dotted number and a title-shaped title. A heading's content is its subtree. The structure stops at `END OF SECTION` and at the footnote, endnote, and header/footer blocks. An empty heading is reported only when its parent isn't empty. The duplicate check reads the same candidates.
- **WP-04B.** Stale-citation suppression uses three tables of cues bound to the citation, with a requirement-verb barrier for the in-clause ones, and each window is cut at the neighboring citations.
- **WP-04C.** The ASCE 7 pattern reads ASCE/SEI, SEI/ASCE, `Standard`, seven dash characters, and four-digit editions, normalized by `_asce7_edition_key` with a century check. The California long forms were added to the California module's vocabulary only.
- **WP-04D.** Whole-word bracket keywords, a bare-TBD pattern placed last, and the identifier rule for `TBD-200`. `[OPTIONAL]` stays a placeholder.
- **WP-04E.** Styles are space, dash, and compact, each with or without SECTION. A majority rule decides dominance, a neutral mixture notice is issued otherwise, and names without a section number stay out. The exporters' shared section intro and the preflight log line were adjusted.
- Tests: `tests/test_heading_structure.py` (63) is new. Added `TestCitationRelatedSuppression`, `TestLocationAwareModulesStillSuppressStaleCycleChecks`, and `TestPreflightNamingLog` to `test_preprocessor_policy.py`; `TestDesignationSyntax` to `test_asce7_stale_editions.py`; and the long-form, placeholder, naming, report, and `TestAlertContractUnchanged` classes to `test_deterministic_checks.py`. The 21 S03 strict xfails (8 markers) became regression tests. The fixtures gained `empty_part`.
- Goldens: `preprocessor_alerts.json` and `dc_preprocessor_alerts.json` each changed one line, the naming alert's `context`. Every structural, stale, placeholder, and invalid-year alert in both is byte-identical.
- Mutation-checked: 37 breakages, every one turned a test red. 12 for headings (bare integers back, each title rule removed, flat emptiness, redundant alerts, leaves instead of ancestors, no structure end, run-in ignored, table rows, a flat duplicate reader, PART whitespace), 9 for suppression (`prior`, `historical`, and any negation back as cues; no requirement-verb barrier; no neighbor bounds; `by` as glue; "as previously" counting; "deviate from" as a rejection verb; bare `not`), 5 for syntax (old ASCE pattern, no century check, no Title 24, no "Edition of the", dashes not separators), 6 for placeholders, and 5 for naming (plurality, unknown names voting, a tie picking a winner, compact or SECTION not read).
- Docs: CLAUDE.md ("Stale-cycle suppression window", the §5 table and a contract paragraph after it, the §9 fixture list). Handbook ch. 4 (a currency note, the detector table, a rewritten suppression section, a new "Reading the heading structure" section, the design-tensions paragraph, and the takeaways). Handbook ch. 15's test-map row. README needed no change; its pre-screen list is still accurate.
- Review: the Codex bot left one P2 finding, and it was fixed. A cue shared by a coordinated list ("Previously, the 2019 CBC and 2019 CMC applied") reached only one member, because each window stopped at the neighboring citation (reproduced first). A list is now judged as one citation. The fix exposed an older phantom citation in comma lists ("CBC, 2019"), which was fixed as well (see "Decisions and deviations"). 5 more mutations (no lists, anything joins a list, paragraph breaks join a list, containment instead of overlap, a member judged alone) were each caught.
- Tests: 3.11 had 4,563 passed, 18 skipped, 23 xfailed. 3.12 with Tk had 4,741 passed, 3 skipped, 26 xfailed. `pip check` was clean, and the JavaScript-required HTML suites passed with `SPEC_CRITIC_REQUIRE_HTML_TEST_TOOLS=1` (the HTML exporter's naming intro changed).
- **Next:** S04.

### 2026-09-24 — S02: Different findings and ambiguous files (WP-06A, WP-07)
- **PR:** [#376](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/376)
- Started at master `af3fc72` (the merge of #375). Both baselines matched "After S01" exactly: 3.11 had 4,159 passed, 18 skipped, 48 xfailed; 3.12 with Tk had 4,337 passed, 3 skipped, 51 xfailed. No failures existed on master.
- **WP-06A.** `_normalize_issue_text` no longer deletes everything from a CSI-shaped number through the next `.docx`. The new `FindingIdentityContext`, an immutable set of the run's exact file names, removes only those names, as literal whole tokens, longest first. With no corpus, the text is kept. `finding_identity_context_for_submission` derives one context from the submission, and the review dedup and the `cf-` / `lc-` stampers each pass it explicitly. The prefixes are unchanged.
- **WP-07.** `_index_specs` maps each name to every distinct supplied file. `_plan_files` binds every document and checks every destination before any document is opened. New outcomes `FILE_AMBIGUOUS` and `DESTINATION_CONFLICT` hold a whole document with a specific reason, and the receipt lists `candidate_paths`. The CLI exits `3` when anything is held this way. `--assist` never sees a held document, and a dry run plans identically.
- Tests: `tests/test_finding_identity_normalization.py` (52) and `tests/test_applier_bindings.py` (39) are new. The four S02 strict xfails were converted to regression tests; the WP-06A one now also runs with Appendix A's known corpus. `test_the_source_is_never_the_destination` now expects `DESTINATION_CONFLICT` and runs in dry-run mode too.
- Mutation-checked: 9 breakages of the normalization (old rule back, no boundaries, shortest first, no escaping, and each stage losing or mis-sourcing its context) and 16 of the applier (first input wins, repeats read as ambiguous, each destination rule, the sidecar-spelling and path rules, the zero-inode guard, the last-line write guard, exit 3 only under `--strict`, order-dependent candidates, and five for the file-identity checks added after review). Every one turned a test red.
- Docs: CLAUDE.md (flow line, the finding-identity paragraph under "Finding-id namespacing", the applier's binding and destination rules and outcome list), README (dedup line and applier bullets), `applier/README.md` (a new "Which file an instruction goes to" section and exit codes), and handbook ch. 7 (the dedup-key row, the defect story, and the heading, which said a false merge was impossible).
- Review: the Codex bot left one P2 finding, and it was fixed. An existing destination that is a hard link to a supplied spec passed the path comparison, so saving rewrote that spec in place (reproduced first). Destinations are now also compared by file identity, in planning and in the last check before the save.
- Tests: 3.11 had 4,256 passed, 18 skipped, 44 xfailed. 3.12 with Tk had 4,434 passed, 3 skipped, 47 xfailed. `pip check` was clean, and the JavaScript-required HTML suites passed with `SPEC_CRITIC_REQUIRE_HTML_TEST_TOOLS=1`.
- **Next:** S03.

### 2026-09-24 — S01: Starting point and shared test fixtures (WP-01)
- **PR:** [#375](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/375)
- Re-measured the starting point at master `6b48503`. It matches the reference (see "Starting point").
- Added `tests/fixtures/spec_docx.py`, the shared DOCX builders. `tests/test_spec_docx_fixtures.py` (55 tests) pins their XML, their determinism, and the ground truth each declares.
- Added `tests/test_contract_pins.py` (97 tests): reconstruction, unique ids, the physical meaning of `pN` / `tN` through the applier's own resolver, established text keeping its id, group-vs-occurrence identity, the repair request re-sending the primary's input, and edits aimed inside Word wrappers being exact-or-refused.
- Converted all 25 checks in `plans/check_plan_status.py` into `tests/test_plan_open_defects.py` and deleted the script. The module holds 51 strict xfails, each naming the chunk that fixes it (3 skip without tkinter), and 29 controls. Added `tests/fixtures/chat_key_probe.js` for the chat-key probe.
- Mutation-checked both directions. Simulated fixes (key kept in page memory, one GUI env write removed, Retry-After honored) turned their xfails into `[XPASS(strict)]` failures while the controls kept passing. Deliberate regressions (`pN` from a paragraph counter, raw merged cells in the applier, the repair suffix moved ahead of the spec) were each caught by the pins.
- Updated CLAUDE.md §9, README "Testing", and handbook ch. 15, which said there was no shared DOCX fixture module.
- Review: the Codex bot left two P2 findings, and both were fixed. The auto-numbered fixture now gets only numbering-neutral pins, so S14 may show labels in the extracted text without breaking a pin. An edit applied inside a wrapper must now keep every wrapper's identity and keep the new text inside it. Self-tests prove the check rejects a removed link and text moved out of one.
- Tests: 3.11: 4,159 passed, 18 skipped, 48 xfailed. 3.12 with Tk: 4,337 passed, 3 skipped, 51 xfailed. `pip check` clean. The JavaScript-required HTML suites pass with `SPEC_CRITIC_REQUIRE_HTML_TEST_TOOLS=1`.
- **Next:** S02.

### 2026-09-23 — Plan revision (before S01)
- **PR:** [#374](https://github.com/Abe-Borg/Claude-Spec-Critic/pull/374)
- Re-checked every package against master `f9da027`; nothing had been implemented, and all defects are still present (plan, Part 3).
- Rewrote the plan as 25 sessions with rules for each session (plan, Parts 1 and 2), and added this tracker and `plans/check_plan_status.py`.
- Corrected the plan's WP-17 item 8. Recorded the starting test result above.
- **Next:** S01.
