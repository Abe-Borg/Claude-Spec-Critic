[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "spec-critic"
version = "1.9.1"
dependencies = [
    "anthropic",
    "python-docx",
    "tiktoken",
    "customtkinter",
    "platformdirs",
    "pymupdf",
]