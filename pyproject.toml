[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "spec-critic"
version = "2.0.0"
dependencies = [
    "anthropic",
    "python-docx",
    "tiktoken",
    "customtkinter",
    "platformdirs",
    "pymupdf",
]